---
title: "The Singularity Space: A Generative Diffusion Framework for Signal Representation"
title_zh: 奇异点空间：一种用于信号表示的生成式扩散框架
authors: "Eli Bar-Yosef, Amir Averbuch, Eli Turkel"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10930v1"
tags: ["query:pc-latent"]
score: 9.0
evidence: 物理约束潜在空间用于逆问题
tldr: "传统生成模型用密集网格表示信号易模糊瞬态特征，本文提出Singularity Space框架，基于复平面奇点表示信号，利用扩散模型预测奇点配置，实现可解释、结构稳定和无分辨率重建。在1D Burgers冲击波任务中，32个奇点替代1024点网格，重建误差降低4.2倍，TV比接近1，物理参数误差达10^{-4}。该工作为语音、生物医学等瞬态主导信号提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10930v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1691, \"height\": 1442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10930v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1334, \"height\": 1237, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10930v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1685, \"height\": 1704, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10930v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1175, \"height\": 921, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10930v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 598, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10930v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1355, \"height\": 265, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10930v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1284, \"height\": 333, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10930v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1017, \"height\": 316, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10930v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1321, \"height\": 764, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10930v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1386, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10930v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1232, \"height\": 718, \"label\": \"Table\"}]"
motivation: 现有生成模型在网格上表示信号会模糊瞬态特征，而物理信号中的尖锐瞬态至关重要。
method: 基于复平面奇点表示的生成框架，用Transformer扩散模型预测带几何约束的奇点配置。
result: "32个奇点替代1024点网格；零样本超分辨率重建误差降低4.2倍；物理参数绝对误差达10^{-4}。"
conclusion: 奇点表示法为瞬态主导信号提供了一种可解释、稳定且无分辨率的生成基础，可扩展至语音、生物医学等领域。
---

## 摘要
生成模型通常将信号表示为振幅的密集网格，这会模糊对物理信号正确性至关重要的尖锐瞬态。我们引入了奇异点空间，这是一种基于复平面奇异点表示信号的生成框架，其根源在于亚纯函数的经典极点-留数表示。我们学习了一个受物理约束的、每个信号独有的奇异点配置的潜在空间，以从降级或部分观测中求解逆问题。该框架具有三个关键特性：可解释性，其中每个生成的奇异点配置对应一组物理参数；结构稳定性，可减轻不连续处的吉布斯伪影；以及无需重新训练或插值即可在任意网格上进行无分辨率输出重建。我们的框架采用基于变换器的扩散模型，该模型直接在复平面奇异点坐标处预测样本，并在采样过程中受几何约束。作为尖锐特征恢复的受控测试案例，我们在1D伯格斯激波上评估了我们的框架，其中每个激波由32个预测奇异点表示（相对于1024点网格信号减少了8倍）。在未见过的测试时观测噪声下，我们的框架保持了信号结构（全变差比≈1），在零样本亚分辨率泛化中实现了比基于网格的基线低4.2倍的重建误差，并在分布内将物理参数恢复到10^{-4}的绝对误差。这些结果表明，基于奇异点的表示可能为其他瞬态主导信号（如语音和生物医学信号）提供实用基础，并具有扩展到更高维领域的潜力。

## Abstract
Generative models often represent signals as dense grids of amplitudes, blurring sharp transients that are crucial for the correctness of physical signals. We introduce Singularity Space, a generative framework that represents signals through complex-plane singularities, rooted in the classical pole-residue representation of meromorphic functions. We learn a latent space of physically constrained, per-signal singularity configurations to solve an inverse problem from degraded or partial observations. The framework has three key properties: interpretability, in which each generated singularity configuration corresponds to a set of physical parameters; structural stability, which mitigates Gibbs artifacts at discontinuities; and resolution-free output reconstruction on arbitrary grids without retraining or interpolation. Our framework employs a transformer-based diffusion model that directly predicts samples at complex-plane singularity coordinates, subject to geometric constraints during sampling. As a controlled test case for sharp-feature recovery, we evaluate our framework on 1D Burgers shocks, where each shock is represented by 32 predicted singularities (an $8\times$ reduction versus a 1024-point grid signal). Our framework preserves signal structure ($\text{TV ratio} \approx 1$) under unseen test-time observation noise, achieves a $4.2\times$ lower reconstruction error in zero-shot sub-resolution generalization than a grid-based baseline, and recovers physical parameters to $10^{-4}$ absolute error in-distribution. These results suggest that singularity-based representations may provide a practical foundation for other transient-dominated signals such as speech and biomedical signals, with potential extension to higher-dimensional domains.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：传统生成模型（如图像/音频扩散模型）将信号表示为密集网格上的振幅值，这会模糊或平滑物理信号中关键的尖锐瞬态（如激波、语音瞬态、生物医学尖峰）。这类网格表示存在“表示失配”：物理现象是局部的，但网络必须在整个网格上协调大量振幅来近似一个瞬态，导致低频偏向和吉布斯振荡。
- **动机**：作者提出一种全新的信号表示范式——**奇异点空间**，利用复平面上离散的孤立奇异点（极点-留数）来表示信号。这源于经典亚纯函数理论：在有限极点下，信号由其极点集唯一确定。这种表示将“瞬态”从密集振幅转换为少数几何坐标，从而绕过网格瓶颈。
- **整体含义**：为瞬态主导信号（如语音、心电、流体激波）提供一种**可解释、结构稳定、无分辨率**的生成框架，有望扩展至更高维领域。

## 2. 论文提出的方法论
### 核心思想
- 用亚纯函数的有限极点-留数表示信号：每个信号对应一个奇异点集合 \( \mathcal{V} = \{(z_k, r_k)\}_{k=1}^N \)，其中 \( z_k \in \mathbb{C} \) 为极点位置（实部对应空间位置，虚部控制瞬态锐度），\( r_k \in \mathbb{C} \) 为留数（控制幅值和相位）。
- 生成过程不再是输出网格值，而是**生成奇异点坐标**，然后通过固定的亚纯解码器（加法展开）解析重建信号。

### 关键技术细节
1. **奇异点坐标对数映射**：为避免 \( y_k \to 0 \) 时的超敏感性，将极点映射到对数空间：  
   \( q_k = [\text{Re}(z_k), \log(\text{Im}(z_k)), \text{Re}(r_k), \text{Im}(r_k)] \in \mathbb{R}^4 \)，从而保证 \( \text{Im}(z_k)>0 \) 并改善梯度。
2. **基于Transformer的扩散模型（Singularity Diffusion Transformer）**：
   - 将 \( N \) 个奇异点视为 \( N \) 个独立令牌，每个令牌特征为 \( q_k \in \mathbb{R}^4 \)，加上可学习的身份嵌入和时间嵌入。
   - 使用自注意力（捕捉奇异点间交互）和交叉注意力（以退化观测为条件），预测干净奇异点集 \( \hat{Q}_0 \)。
3. **直接样本预测（\( \hat{Q}_0 \)-prediction）**而非噪声预测，便于应用几何约束和匈牙利匹配损失。
4. **几何约束投影 \( P_{\Omega_\delta} \)**：在每一步反向扩散后，将奇异点集合投影到有效域 \( \Omega_\delta \)（任意两个极点间距离 ≥ δ），防止极点碰撞（违背简单极点假设）。
5. **损失函数**：包含两部分：
   - 奇异点空间的匈牙利匹配L1损失 \( \mathcal{L}_Q \)。
   - 信号空间的L2重建损失 \( \mathcal{L}_{\text{signal}} \)（通过可微解码器），并采用退火权重 \( \lambda(e) \) 以平衡早期不稳定性。

### 算法流程（文字说明）
- **训练**：给定干净奇异点集 \( Q_0 \) 和退化观测 \( c \)，前向扩散加噪得到 \( Q_t \)；网络 \( f_\theta \) 预测 \( \hat{Q}_0 \)；计算匈牙利匹配损失和重建损失；反向传播。
- **采样**：从高斯噪声 \( Q_T \) 开始，迭代执行：DDPM反向步 → 投影 \( P_{\Omega_\delta} \) → 下一步，直至得到 \( Q_0 \)；最后用解码器 \( \mathcal{D} \) 在任意网格上重建信号。

## 3. 实验设计
### 数据集
- **场景**：1D粘性Burgers激波方程（低黏度极限 \( \nu \sim 10^{-3} \)）。通过解析解生成合成数据，每个激波由三个物理参数（黏度ν、激波强度u0、激波中心xc）确定，但模型从未见到这些参数，仅从退化观测中学习。
- **数据规模**：训练集4000个样本，测试集200个（不同种子）。
- **观测退化**：从原始信号（网格1024点）中随机采样M个空间点，然后线性插值回全网格作为条件输入。M ∈ {16, 32, 64}（分布内），零样本测试M=8和M=128。

### 基准方法
- **FNO**（Fourier Neural Operator）：代表性傅里叶基密集输出基线，训练后直接输出网格重建。
- **AAA**（Adaptive Antoulas-Anderson）：经典有理近似算法，无学习先验，直接拟合退化观测（同样限制32个极点）。

### 评估指标
- L1、L2、L∞误差（点wise）、全变差比（TV ratio，理想为1，衡量结构保持）、位置偏移（X-off）。

## 4. 资源与算力
- **软件**：Python 3.10.19，PyTorch 2.3.0，CUDA 12.1。
- **硬件**：单张NVIDIA RTX 4090或RTX 3070 GPU。
- **训练时长**：论文未明确给出具体训练时间（如小时数）；仅提到batch size=32，early stopping patience=50个epoch。

## 5. 实验数量与充分性
- **实验组数量**：共5组主要实验：
  1. 分布内逆重建（M=16,32,64）。
  2. 零样本多分辨率重建（M=8,128）。
  3. 解码器分辨率独立性（输出网格G=512,2048,4096）。
  4. 测试时噪声鲁棒性（0%, 2%, 5%, 10% RMS噪声）。
  5. 可解释性（物理参数恢复，如位置、极点间距、黏度）。
- **消融实验**：未做典型消融（如损失权重、投影δ、注意力头数等）。但通过**位置对齐诊断**（将预测奇异点平移至真实位置）分离了位置误差与形状误差，相当于一种诊断性分析。
- **充分性与公平性**：
  - 多组条件（不同M、不同噪声）覆盖了分布内和外推情况。
  - 超参数独立调优，固定随机种子确保可复现。
  - 对比了FNO和AAA两个代表性基线。
  - 但缺少对更大噪声、更复杂信号（如时变、平滑叠加）的测试；也没有与更多基于有理近似的学习方法（如LNO）对比。

## 6. 论文的主要结论与发现
1. **分布内重建**：奇异点空间与FNO均能达到低误差，但FNO点wise更优（L2=0.00130 vs 0.00306 at M=16）；然而奇异点空间TV比严格为1，FNO有轻微偏离（1.003-1.013）。AAA因缺乏先验表现差。
2. **零样本泛化**：在未见的M=8（亚分辨率）时，奇异点空间L2=0.085，FNO L2=0.360（降低4.2倍），且TV比接近1（1.003），FNO达1.170；M=128时奇异点空间仍保持稳定。
3. **分辨率无关性**：同一奇异点集在不同输出网格（512,2048,4096）上重建误差几乎不变，验证了解码器的解析连续性。
4. **噪声鲁棒性**：在测试时10%噪声下，FNO的TV比增至2.654，AAA发散至>100；奇异点空间TV比仅1.184，结构保持良好。但点wise误差增大，源于位置偏移（约1/7网格单元）。
5. **可解释性**：模型成功恢复激波位置（误差2×10^{-4}）、所有32个极点的虚部（误差<10^{-4}）和黏度参数（误差<10^{-4}）。生成的不是抽象潜变量，而是直接映射物理量的坐标。
6. **位置漂移问题**：点wise误差主要由亚网格级的位置偏移导致，而非形状失真（对齐诊断后L2从0.582下降至0.145）。

## 7. 优点
- **表示可解释且紧凑**：32个奇异点代替1024个网格点，每个坐标具有物理意义（位置、锐度、幅值）。
- **结构稳定性**：通过亚纯解码器避免吉布斯振荡，在噪声下TV比仍接近1。
- **无分辨率输出**：一旦生成奇异点集，可在任意网格上解析重建，无需重训练或插值。
- **位置-形状误差解耦**：匈牙利匹配损失和对齐诊断可以单独量化两种误差，这是网格输出难以做到的。
- **扩散模型与几何约束结合**：直接样本预测和投影机制确保了奇异点集合的有效性。

## 8. 不足与局限
- **位置漂移敏感**：预测的奇异点集合存在亚网格级位置偏移，导致点wise误差（L2, L∞）较高。虽然结构正确，但在要求精确位置的应用中受限。作者承认这是当前版本的主要局限性。
- **依赖亚纯函数假设**：仅适用于可以用有限简单极点近似的信号。对于含本质奇点、分支点或平滑振荡背景的信号（如语音），需要扩展（如引入全纯背景项h(z)）。
- **输入仍需网格化**：条件观测仍由卷积网络处理固定网格，与“无网格输出”不完全对称。作者提到未来可用坐标-值集合编码器改进。
- **缺乏消融实验**：未系统分析不同损失权重、投影阈值δ、奇异点数量、注意力头数等对性能的影响。
- **仅验证1D稳态激波**：未扩展到时变场景或更高维（如图像、2D物理场）。时间动力学和更高维的奇异点表示是主要开放问题。
- **计算资源报告不充分**：未报告训练时长、模型参数量对比（虽然提及网络参数267K vs FNO 198K，但未讨论推理速度或内存占用）。

（完）
