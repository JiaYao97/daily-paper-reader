Title: Optimal Sensor Placement via Graph-constrained Flow Matching

URL Source: https://arxiv.org/pdf/2607.19831v1

Published Time: Thu, 23 Jul 2026 00:31:49 GMT

Number of Pages: 5

Markdown Content:
# OPTIMAL SENSOR PLACEMENT VIA GRAPH-CONSTRAINED FLOW MATCHING 

# Feng Ji, Jingyang Dai, Wee Peng Tay, Sirajudeen Gulam Razul 

School of Electrical and Electronic Engineering, Nanyang Technological University 

ABSTRACT 

Optimal sensor placement is a fundamental problem in graph signal processing (GSP), where a limited number of sensors are deployed to reconstruct a continuous signal field. Existing GSP methods rely on combinatorial optimization over discretized graphs, resulting in high computational cost and sensor locations restricted to graph vertices. We reformulate sensor placement as a continuous-space generative modeling problem. An offline GSP optimization routine first gener-ates training samples of optimal sensor configurations, from which a flow matching (FM) model learns their distribution. At inference, the learned velocity field directly generates continuous sensor coor-dinates, eliminating online combinatorial optimization. We further develop a permutation-invariant conditional generation framework for deployment with fixed anchor sensors. Experiments on a realis-tic radio propagation simulator demonstrate the effectiveness of the proposed framework for sensor placement. 

Index Terms — Graph signal processing, sensor placement, flow matching, generative model 

1. INTRODUCTION 

Optimal sensor placement is a foundational problem in network engi-neering and graph signal processing (GSP), with critical applications ranging from environmental monitoring and infrastructure security to wireless communications [ 1 , 2, 3]. Given a bounded continuous domain, we consider the task of identifying a sparse subset of spatial locations to deploy a (small) fixed budget of sensors such that the global, continuous signal field can be reconstructed with minimal error. Traditional approaches typically discretize the spatial domain into an ambient graph and invoke greedy combinatorial optimization algorithms [ 4, 5], such as sequential least squares or frame potential maximization, to identify highly informative nodes. Moreover, in practical deployment scenarios, sensor network design is rarely an unconditional, greenfield task. Instead, engineers are frequently confronted with a conditional deployment problem ,which manifests across several modern applications: • Adaptive network expansion [ 6]: An operational region al-ready possesses a set of k1 legacy, static sensors, and an op-erator seeks to deploy k − k1 additional sensors to maximize the expanded network’s reconstruction fidelity under changing spatial field dynamics. • Autonomous drone swarm coordination [ 7]: In mobile sensing or search-and-rescue operations using UAV or drone swarms, a subset of k1 sensing drones may have their positions fixed due to local topographical tracking constraints, battery emergen-cies, or waypoint hovering. The system must instantaneously generate the coordinates for the remaining k − k1 drones to ensure optimal spatial coverage of the signal field. 

> F. Ji and J. Dai contribute equally.

• Obstacle-aware dynamic relocation [ 8]: In complex geomet-ric environments, localized interference might render certain regions blind, requiring adaptive positioning of mobile nodes relative to fixed, unmovable anchor basestations. Solving these placement problems using classical GSP has a few limitations. It requires re-running combinatorial optimization over the entire graph for every unique anchor configuration, and the complexity often scales as a function of the graph size n rather than merely the sensor number k ≪ n. In privacy- or security-sensitive applications, this repeated optimization may require repeated access to the underlying signal data, increasing the risk of unintended information disclosure. Furthermore, discrete algorithms cannot place sensors in the continuous spatial intervals between predefined graph vertices, potentially limiting the network’s performance. To bypass these limitations, we propose a shift in paradigm: rather than treating sensor placement as an online discrete optimiza-tion problem, we frame it as a continuous-space generative inference problem. We introduce a framework that leverages flow matching (FM) [9] over continuous coordinate point clouds. By utilizing an accelerated, regularized GSP oracle strictly during the offline training phase, our generative model learns the underlying continuous proba-bility manifold of optimal sensor configurations. At inference time, the learned deterministic velocity fields guide a set of random noise particles toward optimal sensing coordinates, and the process does not depend on the graph (of size n) anymore. To the best of our knowledge, this is the first work that bridges GSP and continuous generative modeling based on FM for this type of task. Our core contributions are as follows: • We formulate a continuous-space generative framework for sensor placement via GSP-generated supervision that breaks the grid-dependency of traditional graph-based optimization, allowing for super-resolution, off-grid inference. • We introduce a permutation-invariant conditional inference 

pipeline, ensuring that the model seamlessly accommodates pre-fixed physical anchors without retraining. The efficacy of our method is validated via a realistic radio propaga-tion simulator. 

2. PRELIMINARIES 2.1. Graph signal processing 

In this subsection, we briefly review GSP [ 1, 10 , 11 ] concepts most relevant to this paper, with emphasis on signal sampling and recovery. Consider an undirected weighted graph G = ( V, E ) of size 

n = |V |. Let L denote its weighted graph Laplacian, which admits the eigendecomposition L = UΛU ⊤, where the columns of U form an orthonormal eigenbasis of L. A graph signal x ∈ Rn assigns a scalar value to each node in V . It is said to be w-bandlimited if all but w coefficients of its graph Fourier transform bx = U⊤x are arXiv:2607.19831v1 [eess.SP] 22 Jul 2026 zero. Under this assumption, x can be perfectly reconstructed from its values observed at w appropriately selected nodes. Specifically, let V0 ⊂ V be a set of w sampled nodes. Denote by xV0 the subvector of x corresponding to the nodes in V0, and by 

UV0 the w × n submatrix of U whose rows correspond to the nodes in V0. If x is w-bandlimited and U⊤ 

> V0

UV0 is invertible, then x can be recovered exactly from xV0 via 

x = U(U⊤ 

> V0

UV0 )−1U⊤ 

> V0

xV0 . (1) Motivated by the deployment of a limited number of sensors for signal reconstruction over a large spatial region, we use the above formula as the GSP input for our proposed model. 

2.2. Flow matching 

Flow matching (FM) [ 9] is a generative modeling framework designed to transform a simple source distribution (e.g., standard Gaussian noise) into a complex target data distribution. The core intuition behind FM is to find a time-dependent vector field whose paths seamlessly join the source and target distributions. By following this vector field over a time interval t ∈ [0 , 1] , noise points are smoothly pushed along trajectories ϕt until they arrive exactly at the data distribution. Formally, let x ∈ Rd be the state variable. One defines a vector field u : [0 , 1] × Rd → Rd that acts as a velocity field. A starting point x ∼ μ0 from the noise distribution moves over time along ϕt

according to the ordinary differential equation: 

ddt ϕt(x) = ut(ϕt(x)) , ϕ0(x) = x. (2) At t = 1 , the final point ϕ1(x) represents a sample from the generated data distribution μ1(x).To train a neural network uθ (t, x ) to approximate this ideal vector field, one minimizes the difference between our model and a target vector field ut(x):

LFM (θ) = Et,μ t(x)∥uθ (t, x ) − ut(x)∥2. (3) The global target vector field ut(x) depends on the entire data distribu-tion and is intractable. To resolve this, one conditions the trajectories on final data points x1 ∼ μ1(x). This allows one to define straight paths from the source x to x1: xt = tx 1 + (1 − t)x.Since the path is a constant speed straight line, the ideal condi-tional velocity is independent of time and trivial to calculate: ut(x |

x1) = x1 − x. The training objective simplifies to interpolating be-tween noise and data, and training the neural network to predict this constant direction vector x1 − x at any intermediate time t.

2.3. Conditional sensor placement 

We now formally define the problem to be addressed. Consider a continuous region 1 Ω ⊂ R2 with an underlying signal distribution 

μ over the signal space S(Ω) , where each signal is a function on Ω.For f ∼ μ and a set of k sensor locations P = {p1, . . . , p k } ⊂ Ω,we observe the discrete signal fP = ( f (pi)) i≤k ∈ Rk . A problem of interest in signal processing is to find a signal reconstruction function 

Ψ from these observations such that 

Ef ∼μL Ψ( fP ), f  (4)   

> 1Our approach also applies to Ωbeing in a general Riemannian manifold.

is small for a prescribed loss function L. In this paper, we consider the sensor placement problem, which further seeks the sensor locations 

P that minimize the expected reconstruction error in (4) for given Ψ

and L.As discussed in Section 1, many practical applications require the more general setting of conditional deployment. Specifically, suppose that a subset of k1 sensors is fixed at anchor locations Pfix ⊂ Ω. The goal is to determine the remaining k − k1 sensor locations such that the same optimization objective is achieved. To briefly preview our strategy, instead of solving (4) directly, which is generally intractable for arbitrary μ, we adopt a flexible, generative approach. It learns a distribution over sensor locations via solving (4) with Ef ∼μ replaced by an empirical average. The distri-bution assigns a higher probability to configurations with superior reconstruction performance. Sensor deployments are then generated by sampling from this learned distribution. To this end, we first employ GSP to construct a training set for unconditional sensor placement using signals sampled from μ,where the signal reconstruction function Ψ is derived according to Section 2.1. We then train an FM model on these optimized sensor configurations to generate high-quality sensor placements for both the unconditional and anchor-constrained settings. 

3. METHODOLOGY 3.1. Empirical sampling via graph signal processing 

We first outline the overall design of the proposed framework (see Fig. 1). Specifically, we construct a (measurable) function on the signal space Φ : S(Ω) → Ωk , where the codomain Ωk represents the locations of k sensors. Intuitively, Φ maps an input signal f to the sensor locations P whose observations fP yield the best reconstruc-tion of f under a GSP-based reconstruction scheme. The function 

Φ then pushes forward the signal distribution μ on S(Ω) to a dis-tribution Φ∗(μ) on Ωk , i.e., for any measurable subset U ⊂ Ωk ,

Φ∗(μ)( U ) = μ(Φ −1(U )) . Consequently, given samples from μ, the function Φ generates corresponding samples of sensor locations in 

Ωk , which are subsequently used to train an FM model for general-purpose sensor deployment, including anchor-constrained settings. 

Fig. 1 . Φ pushes the signal distribution to a distribution of sensor locations, from which we obtain samples to train the FM model. As the domain Ω is continuous, we first discretize it to incorporate GSP. Specifically, we construct an ambient graph G = ( V, E ), whose size satisfies n ≫ k, via a Voronoi tessellation [ 12 ] that closely approximates Ω. Let V ⊂ Ω be a set of n points forming an ϵ-net ,i.e., for every x ∈ Ω, there exists v ∈ V such that d(x, v ) ≤ ϵ. A sufficiently small hyperparameter ϵ ensures that V provides a faithful approximation of Ω.Given an ϵ-net V = ( v1, . . . , v n), we construct the Voronoi tessellation of Ω as follows. For each vi ∈ V , the (convex) cell Ci

centered at vi consists of all x ∈ Ω satisfying d(x, v i) ≤ d(x, v j )

for every i̸ = j ≤ n. The cells form a partition of Ω and intersect only along their boundaries. We then construct the graph G = ( V, E )with node set V , where two nodes vi, v j ∈ V are connected by an edge whenever Ci ∩ Cj̸ = ∅. Edge weights are assigned using the Gaussian kernel wi,j = exp( −d(vi, v j )2/2c2) for some parameter 

c. The corresponding graph Laplacian is denoted by L = UΛU ⊤,following the notation in Section 2.1. Each signal f ∈ S(Ω) naturally induces a graph signal x on G

by setting x(i) = f (vi). For simplicity, we also denote the induced graph signal distribution by μ.Having established the graph representation, we now apply the GSP framework reviewed in Section 2.1. Given m graph signals 

x1, . . . , xm ∼ μ, we employ a greedy sampling algorithm [ 5] to construct a subset P ⊂ V of sensor locations. We initialize P0 = ∅.At the ℓ-th iteration, where ℓ ≤ k, we update Pℓ = Pℓ−1 ∪ { v∗},where v∗ is obtained by solving 

v∗ = arg min  

> v∈V\P ℓ
> m

X

> j=1

∥xj − U U⊤ 

> Vℓ

UVℓ + λI−1U⊤ 

> Vℓ

xj,V ℓ ∥2, (5) here we write Vℓ = Pℓ−1 ∪ { v} for convenience. After k iterations, the selected sensor locations are given by P.

Discussions . We note that (5) is obtained by modifying (1) with the regularization term λI to ensure matrix invertibility. Consequently, the procedure directly applies the GSP signal reconstruction frame-work described in Section 2.1, using the corresponding signal re-construction function Ψ and reconstruction loss L in (4). Thus, the greedy algorithm addresses (4) when Ef ∼μ is replaced with the aver-age over samples x1, . . . , xm. Furthermore, the computation can be accelerated by updating the matrix inverse via the Sherman–Morrison formula [13]. The above procedure represents the function 

Φ : S(Ω) → V k ⊂ Ωk

outline at the beginning of this subsection. It is repeated for T

iterations, producing a collection of sensor configurations S =

{P (1) , . . . , P(T )}. These samples are from the pushforward distribu-tion Φ∗(μ) (as the target distribution) and constitute the training set for the FM model described in Section 2.2. In the next subsection, we discuss a few outstanding technical issues when applying FM. 

3.2. Sensor placement via flow matching 

Recall that we have obtained a collection of sensor configurations 

S = {P (1) , . . . , P(T )}. Replacing the graph nodes in each P(i)

with their corresponding spatial coordinates, we regard the target distribution of the generative model as being supported on Ωk , or 

Ωk−k1 when k1 anchor sensors are fixed. Although FM has been reviewed in Section 2.2, we provide additional technical details here to address three practical issues: (a) permutation invariance of sensor configurations; (b) anchor-constrained sensor placement; and (c) the encoder architecture for the vector field uθ in (3). Let Pfix = {p∗ 

> 1

, . . . , p ∗ 

> k1

} denote the fixed anchor sensor loca-tions. Starting from a noisy sample P0 = {p0,1, . . . , p 0,k }, our goal is to generate P1 = {p1,1, . . . , p 1,k } such that Pfix is a subset of P1.Since sensor configurations are unordered sets whereas FM generates an ordered sequence of coordinates, it is unclear which generated sensors should correspond to the prescribed anchors. To resolve this ambiguity, we perform a spatial matching step at t = 0 . Specifically, we solve the linear assignment problem (e.g., using the Hungarian algorithm [14]): 

π∗ = arg min 

> πk1

X

> j=1

∥p0,π (j) − p∗ 

> j

∥2,

where π is a permutation of the k sensor indices. This assigns each anchor to its nearest sensor in the initial noisy configuration, allowing the ODE trajectories in (2) to evolve along paths of minimal displace-ment while avoiding undesirable global index-shuffling artifacts. Having established the correspondence, we henceforth treat the sensor sets Pt, t ∈ [0 , 1] , as ordered. Following the assignment π∗,the matched sensors are gradually guided toward their target anchors through a sigmoidal blending schedule [15]: 

σt =  1 + e−κ(t−t0)−1,

which interpolates between the FM-generated sensor location and the target anchor location. The modified trajectory for a matched sensor 

i = π∗(j) ∈ Im (π∗) at time t is 

pt,i = (1 − σt)ˆ pt,i + σtp∗ 

> j

,

where ˆpt,i denotes the sensor location generated by FM at time t.Choosing t0 = 0 .5 and κ = 10 ensures that the sensors evolve almost freely at the beginning of the trajectory, while satisfying p1,i ≈ p∗

> j

at t = 1 as required. The remaining k − k1 sensors evolve freely throughout the entire process, and together with the anchor sensors constitute the final sensor configuration P1.While we have completed the overall framework, it remains to explicitly describe the encoder architecture for the velocity field uθ

in (3). We model the velocity field using a point transformer [ 16 ]. Specifically, each sensor coordinate pt,i interacts with the entire sensor configuration through a multi-head attention: 

ht,i = Attention 



Query (pt,i , t ), Keys ({pt,j }j≤k , t ),

Values ({pt,j }j≤k , t )



.

The resulting feature vector is then passed through an MLP , which outputs the velocity vector uθ (t, p i,t ) at pi,t . The parameter set θ

comprises all parameters in the multi-head attention module together with those of the final MLP. By omitting positional encodings, the model is permutation equiv-ariant, ensuring that the velocity of each sensor depends solely on its spatial relationship with the remaining sensors in the configuration. 

4. NUMERICAL RESULTS 

In the numerical study, we consider the communication sensor net-work described in [ 17 ]. Specifically, there is a flat 2D area Ω with 3 wireless transmitters, and their locations jointly follow a mixed Gaus-sian distribution with two peaks (see Fig. 2(a) for an illustration). The signal received at each location from a single transmitter is subject to path loss, shadow fading, and fast fading. The explicit formula can be found in [ 17 , 18 ], and we show an example in Fig. 2(b). Further-more, the received signal is subject to additive Gaussian noise. The uncertainty in the transmitter locations and signal reading collectively gives the signal distribution μ.In this study, we want to place 10 sensors to measure and recon-struct the communication signals, and further assume 5 of them are pre-determined anchors whose locations are generated according to different schemes as discussed below. To implement our scheme, we let V be the uniformly spaced 

100 points in Ω, and the Voronoi tessellation generates the standard 

10 × 10 lattice graph G = ( V, E ). Using GSP outlined in Section 3.1, we generate a collection of sensor configurations S with T = 1600 

samples. FM described in Section 3.2 allows us to identify the loca-tions of 5 sensors to be optimally placed. In addition, we generate another collection S0 of 200 configurations for testing and evaluation. 0 0.25 0.5 0.75 1

> X
> 0
> 0.25
> 0.5
> 0.75
> 1
> Y

(a) Samples from the 

transmitter location prior     

> Gaussian component 1
> Gaussian component 2
> 00.25 0.5 0.75 1
> X
> 0
> 0.25
> 0.5
> 0.75
> 1
> Y

(b) Expected signal strength 

with three transmitters  

> -12 dB
> -12 dB
> -12 dB
> -8 dB
> -8 dB
> -8 dB
> Transmitters 14
> 12
> 10
> 8
> 6
> Received signal strength (dB)

Fig. 2 . In (a), we show the empirical approximation of a two-peak Gaussian mixture for the locations of 3 transmitters, called “triangle”. In (b), we give an example of the sample locations of the transmitters (from the Gaussian mixture) and the resulting signal over Ω.0 0.25 0.5 0.75 1    

> X
> 0
> 0.25
> 0.5
> 0.75
> 1
> Y Gaussian component 1
> Gaussian component 2 00.25 0.5 0.75 1
> X
> 0
> 0.25
> 0.5
> 0.75
> 1
> Y
> Gaussian component 1
> Gaussian component 2

Fig. 3 . We show the transmitter joint distributions “Lshape” and “diagonal” used for evaluation. We compare the following experimental settings: • GB: The golden baseline S0.• SF: The anchor sensor locations are chosen from s amples in 

S0, and the remaining sensors are deployed according to FMin Section 3.2. Under this setting, the anchors are reasonably placed according to the signal distribution. • SP: The anchor sensor locations are chosen from s amples in 

S0, and the remaining sensors are deployed using PRODIGY [19], a benchmark generative model under constraints. • RF: The anchor sensor locations are r andom, and the remain-ing sensors are deployed according to F M in Section 3.2. Un-der this setting, the anchors can be suboptimally placed as there is no input from the signal distribution. • SR: The anchor sensor locations are chosen from s amples in 

S0, and the remaining sensors are deployed randomly. • AR: All sensors are placed randomly. Notice that the generated locations have continuous coordinates. For evaluation, we project them to their nearest node in the graph 

G, and the resulting node set is V10 . We consider the following two metrics: • A-optimality (cf. (5)): tr  (U⊤ 

> V10

UV10 + λI)−1. This is a generic metric for bandlimited graph signals [ 4]. It does not take input from the distribution, but relies on the observation that the resulting signals on G are usually smooth, as shown in Fig. 2(b). • Empirical relative mean squared error (RMSE) for reconstruc-tion: we draw signals x1, . . . , x50 according to μ. Let P∗ be the sensor configuration solved according to the greedy algo-rithm in Section 3.1 without constraints, treated as the ground truth. Its squared reconstruction error (the summation in (5) for m = 50 ) is denoted by ϵ∗. For each of the above settings, we compute the corresponding squared reconstruction error as 

ϵ. Then the RMSE is ϵ/ϵ ∗.

Table 1 . A-optimality gap: difference with GB; and RMSE. The best result other than GB in each row is boldfaced .GB SF SP RF SR AR triangle 0 13.2 20.6 38.4 119.6 196.3 Lshape 0 11.8 12.6 36.7 115.8 193.3 diagonal 0 17.9 22.7 43.9 149.7 192.8 triangle 1.15 1.27 1.46 1.46 1.97 2.25 Lshape 1.15 1.26 1.32 1.47 2.57 1.98 diagonal 1.23 1.35 1.34 1.93 2.54 2.59 We run the experiments for 3 distinct distributions of the trans-mitter locations in Figs. 2 and 3: triangle, Lshape, and diagonal. The results, averaged over 200 runs, are summarized in Table 1. For either metric, we see that in general, being close to GB, our proposed model performs the best when the anchors are from S0. This suggests that our generative approach almost faithfully recovers the sensor location distribution, thereby selecting the best possible locations. It is also interesting to notice that RF always outperforms SR, though RF has suboptimal anchor locations. This suggests that our model rectifies the suboptimality of the constraints via the dynamics of the generative process for the newly deployed sensors (see Fig. 4 for an example). In summary, the learned distribution compensates for poor anchors. 0 0.25 0.5 0.75 1

> X
> 0
> 0.25
> 0.5
> 0.75
> 1
> Y

(a) RF     

> 00.25 0.5 0.75 1
> X
> 0
> 0.25
> 0.5
> 0.75
> 1
> Y

(b) SR   

> Transmitters Sensors Anchors

Fig. 4 . In this example, RF places sensors to effectively “fill in the gaps” of the anchor configuration (e.g., around the top-right transmit-ter). In contrast, SR cannot consistently achieve this, as the additional sensors are placed randomly. 

5. CONCLUSIONS 

We proposed a continuous-space generative framework for sensor placement that leverages GSP-generated training samples and flow matching to eliminate online combinatorial optimization. The pro-posed method supports both unconditional and anchor-constrained deployment through a permutation-invariant conditional generation framework. Experimental results on realistic radio propagation data demonstrate its effectiveness and flexibility for continuous sensor placement. 6. REFERENCES 

[1] D. I. Shuman, S. K. Narang, P. Frossard, A. Ortega, and P. Van-dergheynst, “The emerging field of signal processing on graphs: Extending high-dimensional data analysis to networks and other irregular domains,” IEEE Signal Process. Mag. , vol. 30, no. 3, pp. 83–98, 2013. [2] N. Sahu, L. Wu, P. Babu, B. S. M. R., and B. Ottersten, “Opti-mal sensor placement for source localization: A unified admm approach,” IEEE Trans. Veh. Technol. , vol. 71, no. 4, pp. 4359– 4372, 2022. [3] Y. Ma, H. Wu, H. Zhou, H. Weng, J. Wang, and M. Long, “PhySense: Sensor placement optimization for accurate physics sensing,” in NeurIPS , 2025. [4] A. Anis, A. Gadde, and A. Ortega, “Efficient sampling set selection for bandlimited graph signals using graph spectral proxies,” IEEE Trans. Signal Process. , vol. 64, no. 14, pp. 3775– 3789, 2016. [5] L. F. O. Chamon and A. Ribeiro, “Greedy sampling of graph signals,” IEEE Trans. Signal Process. , vol. 66, no. 1, pp. 34–47, 2018. [6] R. Berner, T. Gross, C. Kuehn, J. Kurths, and S. Yanchuk, “Adaptive dynamical networks,” Physics Reports , vol. 1031, pp. 1–59, 2023. [7] R. Chandrashekar, G. Sowjanya, M. Gupta, G. Karuna, S. M. Shareef, and A. Kaushik, “Simulation of autonomous drone swarm coordination and intelligent path planning leveraging reinforcement learning with geospatial data integration,” in 

RCSM , 2025. [8] A. Costa, P. Duarte, A. Coelho, and R. Campos, “Obstacle-aware positioning of a mobile robotic platform for next-generation wireless networks,” in WiMob , 2024. [9] Y. Lipman, R. T. Q. Chen, H. Ben-Hamu, M. Nickel, and M. Le, “Flow matching for generative modeling,” in ICLR , 2023. [10] A. Ortega, P. Frossard, J. Kovaˇ cevi´ c, J. Moura, and P. Van-dergheynst, “Graph signal processing: Overview, challenges, and applications,” Proc. IEEE , vol. 106, no. 5, pp. 808–828, 2018. [11] X. Jian, F. Ji, and W. P. Tay, “Generalizing graph signal process-ing: High dimensional spaces, models and structures,” Foun-dations and Trends® in Signal Processing , vol. 17, no. 3, pp. 209–290, 2023. [12] F. Aurenhammer, “Voronoi diagrams – a survey of a fundamen-tal geometric data structure,” ACM Comput. Surv. , vol. 23, no. 3, p. 345–405, 1991. [13] J. Sherman and W. J. Morrison, “Adjustment of an inverse matrix corresponding to a change in one element of a given matrix,” Ann. Math. Stat. , vol. 21, no. 1, pp. 124–127, 1950. [14] H. W. Kuhn, “The hungarian method for the assignment prob-lem,” Nav. Res. Logist. Q. , vol. 2, no. 1-2, pp. 83–97, 1955. [15] P. Sergio Rodrigues, G. Wachs-Lopes, R. Morello Santos, E. Coltri, and G. Antonio Giraldi, “A q-extension of sigmoid functions and the application for enhancement of ultrasound images,” Entropy , vol. 21, no. 4, 2019. [16] A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez, L. Kaiser, and I. Polosukhin, “Attention is all you need,” in NeurIPS , 2017. [17] X. Jian, M. Gölz, F. Ji, W. P. Tay, and A. M. Zoubir, “A graph signal processing perspective of network multiple hypothesis testing with false discovery rate control,” IEEE Trans. Signal Process. , vol. 73, pp. 3496–3512, 2025. [18] X. Cai and G. Giannakis, “A two-dimensional channel simula-tion model for shadowing processes,” IEEE Trans. Veh. Technol ,vol. 52, no. 6, pp. 1558–1567, 2003. [19] K. Sharma, S. Kumar, and R. S. Trivedi, “Diffuse, sample, project: plug-and-play controllable graph generation,” in ICML ,2024.