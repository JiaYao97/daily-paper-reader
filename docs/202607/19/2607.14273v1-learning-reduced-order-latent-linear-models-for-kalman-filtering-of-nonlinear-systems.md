---
title: Learning reduced-order latent linear models for Kalman filtering of nonlinear systems
title_zh: 学习用于非线性系统卡尔曼滤波的降阶潜在线性模型
authors: "Manas Mejari, Milad Banitalebi Dehkordi, Dario Piga"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.14273v1"
tags: ["query:pc-latent"]
score: 6.0
evidence: 自编码器学习潜在动力学用于状态估计
tldr: 针对高维非线性系统状态估计中降阶模型与滤波器设计分离的问题，提出面向滤波的端到端学习框架。通过自编码器学习低维潜在表示和线性动力学，联合最小化重建误差与可微卡尔曼滤波损失，使降阶模型专用于滤波任务。推理时在潜在空间执行滤波并解码重建全状态。理论方面利用共形预测量化全阶与降阶滤波器的性能差距概率界。在热扩散基准上验证从稀疏测量重构完整温度场的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.14273v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 904, \"height\": 667, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14273v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 896, \"height\": 668, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14273v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 905, \"height\": 362, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.14273v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 898, \"height\": 358, \"label\": \"Figure\"}]"
motivation: 传统两步法先识别降阶模型再设计滤波器，模型与任务不匹配，滤波性能受限。
method: 联合训练自编码器与降阶线性时不变模型，损失函数融合重建误差和可微卡尔曼滤波目标。
result: 在热扩散基准上成功从稀疏测量重构完整温度场，并提供性能差距的概率保证。
conclusion: 端到端学习面向滤波的降阶模型，能有效提升非线性系统状态估计性能且有理论支撑。
---

## 摘要
我们提出了一种面向滤波的端到端学习框架，用于识别针对高维非线性系统中状态估计而明确定制的降阶模型。自编码器（AE）神经网络学习状态的低维潜在表示以及到原始空间的提升映射，同时一个降阶线性时不变（RO-LTI）模型描述潜在动力学。通过最小化一个多目标损失函数来联合训练AE和RO-LTI模型，该损失函数结合了重建误差和基于可微卡尔曼滤波器的滤波目标，确保降阶模型是为下游状态估计任务定制的。在推理时，滤波完全使用RO-LTI模型在潜在空间中进行，并通过解码器将估计的状态映射回原始空间。与传统的两阶段方法不同（先为系统近似识别降阶模型，然后在其上设计滤波器），所提出的框架学习一个面向任务的降阶模型，其参数完全由滤波性能而非仅系统近似精度决定。我们进一步通过保形预测量化了全阶和降阶滤波器之间性能差距的概率界限，该方法不需要对数据分布做假设。该方法在热扩散基准上进行了验证，其中从稀疏测量中重建完整的温度场。

## 速览
**TLDR**：高维非线性系统的状态估计中，传统方法先识别降阶模型再设计滤波器，两者目标分离导致性能欠佳。本文提出端到端学习框架，联合训练自编码器和降阶线性时不变模型，通过可微卡尔曼滤波优化滤波损失。在热扩散基准上从稀疏测量成功重建完整温度场，并利用共形预测量化了与全阶滤波器的性能差距概率界。该方法使降阶模型直接服务于状态估计任务，显著优于两阶段方案，且无需数据分布假设。 \
**Motivation**：传统两阶段方法中降阶模型与滤波器分离，降阶模型未针对状态估计优化，影响滤波精度。 \
**Method**：联合训练自编码器与降阶线性模型，最小化重构误差和可微卡尔曼滤波损失，端到端学习面向滤波的潜在动态。 \
**Result**：在热扩散基准上，从稀疏测量重建完整温度场，滤波性能优于两阶段方法，并给出了概率性能界。 \
**Conclusion**：提出任务导向降阶建模，使模型参数由滤波性能驱动，结合共形预测实现可靠估计。

---

## Abstract
We propose a filtering-oriented end-to-end learning framework to identify reduced-order models explicitly tailored for state estimation in high-dimensional nonlinear systems. An autoencoder (AE) neural network learns a low-dimensional latent representation of the state together with a lifting map to the original space, while a reduced-order linear time-invariant (RO-LTI) model describes the latent dynamics. The AE and RO-LTI model are trained jointly by minimizing a multi-objective loss that combines reconstruction error with a filtering objective based on a differentiable Kalman filter, ensuring that the reduced-order model is tailored for the downstream state estimation task. At inference, filtering is performed entirely in the latent space using the RO-LTI model, and the estimated state is mapped back to the original space via the decoder. Unlike conventional two-stage approaches, in which a reduced-order model is first identified for system approximation and a filter is subsequently designed on top of it, the proposed framework learns a task-oriented reduced-order model whose parameters are shaped entirely by filtering performance rather than system approximation accuracy alone. We further quantify probabilistic bounds on the performance gap between full-order and reduced-order filters using conformal predictions, which do not require assumption on data distribution. The approach is validated on a heat diffusion benchmark, where the full temperature field is reconstructed from sparse measurements.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：高维非线性系统（如流体动力学、电力系统、机器人）的状态估计是控制与信号处理的核心问题。卡尔曼滤波器及其变体虽提供经典框架，但直接应用于高维系统时计算负担巨大（需传播高维状态和协方差矩阵），难以在嵌入式或实时场景中部署。
- **背景问题**：传统降阶方法（如平衡截断、奇异摄动）主要针对线性系统且需要显式模型；数据驱动降阶方法（如主成分分析、自编码器）虽能学习紧凑潜在表示，但通常仅优化重建或预测精度，未考虑下游状态估计（滤波）任务。这导致两阶段范式：先独立学习降阶模型，再在其上设计滤波器，两者目标分离，滤波性能受限。
- **本文核心挑战**：如何学习一个**面向滤波任务**的降阶模型，使其参数完全由滤波性能驱动，而非仅系统逼近精度。

#### 2. 方法论：核心思想、关键技术细节、算法流程
- **核心思想**：
  - 通过自编码器（Autoencoder, AE）学习低维潜在表示（维度 \(n_z \ll n_x\)）和对应的提升映射（解码器 \(\Phi: \mathbb{R}^{n_z} \to \mathbb{R}^{n_x}\)）。
  - 在潜在空间使用降阶线性时不变（RO-LTI）模型描述动态：\(z_{k+1} = A z_k + B u_k + \tilde{w}_k,\ y_k = C z_k + D u_k + \tilde{v}_k\)。
  - 将可微卡尔曼滤波器（KF）嵌入训练循环，联合优化模型参数 \(\theta = \{A, B, C, D, Q, R\}\)（噪声协方差通过Cholesky分解参数化保证半正定）和AE参数 \((\psi, \phi)\)。
- **关键技术细节**：
  - **多目标损失函数**：$\mathcal{L} = \lambda_{\text{ae}} \mathcal{L}_{\text{ae}} + \lambda_{\text{nll}} \mathcal{L}_{\text{nll}} + \lambda_{\text{filt}} \mathcal{L}_{\text{filt}} + \lambda_{\text{latent}} \mathcal{L}_{\text{latent}}$
    - \(\mathcal{L}_{\text{ae}}\)：重建误差（AE输出与真实状态）。
    - \(\mathcal{L}_{\text{nll}}\)：卡尔曼滤波负对数似然（基于新息残差及其协方差），鼓励模型与观测一致。
    - \(\mathcal{L}_{\text{filt}}\)：解码后状态估计误差（原始空间），直接优化滤波精度。
    - \(\mathcal{L}_{\text{latent}}\)：潜在对齐损失（AE编码器输出与KF后验均值的一致性）。
  - **训练三阶段**：先重点学习AE（前150 epoch），再逐步引入KF负对数似然（中间100 epoch），最后固定AE重建项并同时优化所有项（后100 epoch）。
  - **推理阶段**：仅需潜在空间KF + 解码器，编码器不再需要。
- **理论贡献**：基于共形预测（Conformal Prediction）提供**无分布假设**的概率性能界：对于新样本，降阶滤波器与全阶滤波器的性能差距（RMSE差）以至少 \(1-\alpha\) 概率低于一个由验证集计算的阶统计量 \(\Psi^{(k^*)}\)，该结论以至少 \(1-\delta\) 置信成立。

#### 3. 实验设计：数据集/场景、benchmark、对比方法
- **数据集/场景**：一维非线性热扩散问题（钢棒），空间离散为100个节点（状态维度 \(n_x=100\)），5个传感器（\(n_y=5\)），2个控制输入（边界温度）。训练数据：多正弦边界输入；测试数据：单正弦边界输入；验证集用于概率界：1000个独立轨迹（各500步）。
- **Benchmark**：全阶扩展卡尔曼滤波（FOEKF）作为“oracle”，使用真实非线性模型和噪声协方差。
- **对比方法**：
  - **SIDKF**：两阶段方法，先基于系统识别损失（预测误差+重建）学习RO-LTI模型和AE，再手动设置噪声协方差进行KF（无KF在训练循环中）。
  - **ROKF**（本文方法）：面向滤波学习的降阶卡尔曼滤波器（潜在维度 \(n_z=8\)）。
- **评估指标**：均方根误差（RMSE），计算1000次蒙特卡洛试验。

#### 4. 资源与算力
- **文中未明确说明**：未提及GPU型号、数量、训练时长。仅提到使用Adam优化器，学习率 \(10^{-2}\)，训练350 epoch。

#### 5. 实验数量与充分性
- **实验数量**：
  - 对比实验：1000次蒙特卡洛试验（图3箱线图）比较FOEKF、ROKF、SIDKF的RMSE。
  - 概率界验证：1000个独立验证集计算阶统计量，再用500个独立测试集验证（图4直方图）。
  - 未见显式的消融实验（如不同损失权重的影响、不同潜在维度）。
- **充分性与客观性**：
  - 对比方法SIDKF设计合理（用相同AE架构，仅训练目标不同），且FOEKF作为Oracle提供了上界。
  - 蒙特卡洛试验次数充足，结果统计显著（图3显示ROKF明显优于SIDKF）。
  - 概率界验证结果与理论一致（494/500满足界，超过95%）。
  - **局限**：仅在一个热扩散基准上测试，缺少多类非线性系统（如流体、机器人）的验证；未研究潜在维度选择、训练超参数敏感度等；未对比其他降阶滤波方法（如EnKF、Koopman-based方法）。

#### 6. 主要结论与发现
- 面向滤波学习的ROKF从5个稀疏传感器重建100维温度场，平均RMSE约2°C，性能接近FOEKF（Oracle），远优于两阶段SIDKF。
- 联合优化AE和RO-LTI模型（包含噪声协方差）能显著提升滤波精度，表明任务导向降阶建模的有效性。
- 共形预测成功提供了性能差距的分布无关概率界（95%概率下差距≤3.23°C），在500个测试集中98.8%符合该界。

#### 7. 优点
- **端到端任务导向学习**：打破两阶段分离，使降阶模型参数由滤波性能直接塑造。
- **可微卡尔曼滤波嵌入**：允许通过自动微分计算梯度，联合优化模型与噪声协方差（SIDKF手动设定噪声）。
- **理论保障**：共形预测提供无假设的概率性能界，增加部署可靠性。
- **推理高效**：仅在8维潜在空间运行KF，计算量远小于全阶EKF。

#### 8. 不足与局限
- **实验覆盖不足**：仅测试单一热扩散基准（线性+弱非线性），未在强非线性或混沌系统（如流体、机器人）上验证泛化性。
- **缺乏消融与分析**：未讨论不同损失权重、潜在维度、训练阶段策略的影响；未比较不同AE架构（如卷积、RNN）的效果。
- **潜在线性假设限制**：RO-LTI模型假设潜在动态线性，对本质非线性系统可能失效；未探索更丰富的潜在模型（如LPV）。
- **方法依赖全状态训练**：训练时需要完整状态 \(x_k\)（实际中可能难以获取），推理时虽不需要但限制了应用场景。
- **概率界依赖验证集**：需要足够多的验证样本（本文1000个），小样本情况下界可能松弛。

（完）
