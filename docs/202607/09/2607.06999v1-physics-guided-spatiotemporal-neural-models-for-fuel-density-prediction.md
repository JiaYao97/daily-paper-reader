---
title: Physics-guided spatiotemporal neural models for fuel density prediction
title_zh: 物理引导的时空神经模型用于燃料密度预测
authors: "Tolga Caglar, Jaynil Jaiswal, Saqib Azim, Yudhir Gala, Mai H. Nguyen, Ilkay Altintas"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.06999v1"
tags: ["query:flow-pred"]
score: 8.0
evidence: 物理引导的时空神经模型用于燃料密度预测，集成了神经算子和视频transformer
tldr: 燃料密度预测对火灾管理至关重要，但纯数据驱动模型缺乏物理一致性。本文提出物理引导机器学习框架，在ConvLSTM、AFNONet和ViViT模型中融入质量守恒与蔓延速率物理约束。实验表明，该框架在多个独立试验中预测准确性和稳定性均优于纯数据驱动基线。该工作实现了高效、物理合理的火灾预测，可支持自适应预设燃烧管理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06999v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 913, \"height\": 505, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06999v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1871, \"height\": 472, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06999v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1553, \"height\": 332, \"label\": \"Table\"}]"
motivation: 现有数据驱动燃料密度预测模型缺乏物理约束，导致准确性和稳定性不足，亟需融合物理知识提升性能。
method: 提出物理引导机器学习框架，在ConvLSTM、AFNONet、ViViT的损失函数中加入可微的质量守恒燃料传输项和蔓延速率估计项。
result: PGML框架在多个独立试验中，预测准确性和稳定性均显著优于无物理约束的纯数据驱动基线。
conclusion: 实现了高效、物理合理的火灾预测，为自适应预设燃烧管理提供可靠支持。
---

## 摘要
本文提出了一种物理引导的机器学习（PGML）框架用于燃料密度预测，将物理约束和领域知识融入深度学习模型，以提升模型的准确性和稳定性。我们探索了三种深度学习架构——ConvLSTM、自适应傅里叶神经算子（AFNONet）和视频视觉变换器（ViViT）——来模拟燃料密度的时空演化。我们的方法在损失函数中加入了可微的物理信息项，包括质量守恒的燃料传输项和扩散速率估计。多个独立试验的平均实验结果表明，所提出的PGML框架在没有物理约束的纯数据驱动基线方法中，在准确性和稳定性方面均表现更优。该框架能够实现计算高效且物理上合理的火灾预测，以支持自适应预设燃烧管理。

## Abstract
This paper presents a physics-guided machine learning (PGML) framework for fuel density prediction, integrating physics constraints and domain knowledge into deep learning models to enhance model accuracy and stability. We explore three deep learning architectures -- ConvLSTM, Adaptive Fourier Neural Operator (AFNONet), and Video Vision Transformer (ViViT) -- to model the spatiotemporal evolution of fuel density. Our approach incorporates differentiable physics-informed terms in the loss function, including a mass-conserving fuel transport term and a rate-of-spread estimation. Experimental results, averaged across multiple independent trials, demonstrate that the proposed PGML framework outperforms purely data-driven baselines without physics constraints in both accuracy and stability. This framework enables computationally efficient, physically plausible fire forecasting to support adaptive prescribed burn management.

---

## 论文详细总结（自动生成）

# 论文总结：物理引导的时空神经模型用于燃料密度预测

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：野火每年在美国烧毁数百万英亩土地，威胁生态、生命和财产。为减轻风险，消防管理者采用预设燃烧（prescribed burns）来减少燃料负荷。传统基于过程的模型（如FARSITE、QUIC-Fire）能模拟火行为，但计算成本高，无法支持实时决策。
- **问题**：纯数据驱动的深度学习模型（如CNN、U-Net）虽然计算快，但缺乏物理一致性，常预测出非物理行为（如燃料自发再生），且难以处理复杂多点点火场景。
- **整体含义**：本文旨在构建一个物理引导的机器学习（PGML）框架，通过将物理约束作为软惩罚融入损失函数，在不牺牲计算效率的前提下提升模型预测的物理合理性和稳定性，从而支持自适应预设燃烧管理。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：在三个代表性深度学习架构（ConvLSTM、AFNONet、ViViT）中，引入可微的物理约束项作为损失函数的组成部分，以“软约束”方式指导模型学习物理一致的燃料密度演化，避免传统PDE硬约束带来的数值不稳定性。

- **关键技术细节**：
  - **三种基础架构**：
    - **ConvLSTM**：2层结构（64个3×3卷积核），添加残差连接（3D卷积头）和时空平滑（3×3×3卷积核），使用LeakyReLU防止稀疏数据下的神经元死亡。
    - **ViViT**：将输入帧分割为3D tubelet（t=3, h=4, w=4），通过自注意力建模时空依赖，解码时用MLP将token映射回tubelet并重构完整视频。
    - **AFNONet**：采用3D patch嵌入（8×8×3的tubelet），通过傅里叶域混合（O(N log N)复杂度）替代自注意力，直接映射输入序列到输出序列（Seq2Seq）。
  - **物理引导损失函数（WiFireLoss）**：
    - **Base MSE（L_MSE）**：标准像素级均方误差。
    - **Fuel Transport（L_fuel）**：惩罚燃料密度随时间增加的区域（物理上燃料只消耗不生成），对正梯度像素计算MSE。
    - **State-Weighted Losses（L_burn, L_unburn）**：通过可微温度缩放sigmoid生成掩膜（阈值F*=0.665 kg/m²），分别对燃烧区和未燃烧区加权计算MSE。
    - **Rate of Spread（L_ROS）**：用可微方式近似火灾前沿位置（通过softmax加权）和蔓延速度，约束预测的ROS与真实ROS之间的误差。
    - **总损失**：L_total = λ_MSE L_MSE + λ_fuel L_fuel + λ_burn L_burn + λ_unburn L_unburn + λ_ROS L_ROS，权重设为 λ_MSE=1, λ_fuel=0.01, λ_ROS=0.01, λ_burn=0.1, λ_unburn=0.1。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：使用QUIC-Fire模拟的火灾蔓延数据，每段模拟时长50秒，空间网格300×300（平坦草地）。包含所有7种风速（1-15 m/s）、11种风向（230°-330°）、4种点火模式（aerial, inward, outward, strip north）的组合。燃料密度范围[0, 0.7 kg/m²]。预处理包括风速风向的min-max归一化、加入源图（source map）作为额外通道。输入6帧（T_in=6）预测后3帧（T_out=3），时序步长为1。
- **基准**：对每种架构，均对比**无物理约束的基线模型**（纯MSE训练）和**带物理引导损失的PG模型**（PG-ConvLSTM, PG-AFNONet, PG-ViViT）。
- **对比方法**：ConvLSTM、AFNONet、ViViT三个架构内部自身对比（有/无物理损失），未与其他公开方法（如PINNs）对比。
- **评估指标**：Base MSE、Burn MSE、Unburn MSE、Fuel Transport、ROS误差、Total Loss（均为均值±标准差，3次独立运行）。

## 4. 资源与算力

- **硬件**：单块NVIDIA RTX 3090 Ti GPU。
- **精度**：使用bfloat16混合精度训练。
- **优化器**：AdamW，余弦退火学习率（峰值1e-3 → 最小值1e-5）。
- **训练轮数**：ConvLSTM训练15 epochs，早停耐心5；AFNONet训练50 epochs，早停耐心10；ViViT训练轮数未明确说明（推测约50 epochs）。批大小：ConvLSTM=4，AFNONet=8，ViViT未明确。
- **总训练时长**：文中未具体给出。

## 5. 实验数量与充分性

- **实验组数**：共6组实验（3种架构 × 有无物理损失），每组重复3次，报告均值和标准差。总计18次训练。
- **充分性分析**：
  - **积极方面**：在三种不同架构上均验证了物理引导的有效性，证明了方法的通用性；重复实验降低了随机性影响；使用了模拟数据的完整组合。
  - **不足**：（1）未进行消融实验探讨各损失项权重的影响；（2）未与其他物理约束方法（如PINNs、方程约束方法）直接对比；（3）仅在单一模拟场景（平坦草地）上测试，未涉及真实火灾数据、多种燃料类型或复杂地形；（4）预测窗口短（3帧），未评估长序列性能。
  - **公平性**：基线模型与PG模型除损失函数外其他设置一致，对比相对公平。

## 6. 论文的主要结论与发现

1. **物理引导损失显著提升了预测性能**：在所有三种架构中，PG模型的Base MSE、Burn MSE、Unburn MSE、Fuel Transport、ROS误差及总损失均低于纯数据驱动基线，且标准差更小，表明预测更准确、更稳定。
2. **通用性**：该PGML框架可无缝应用于ConvLSTM、AFNONet、ViViT等不同架构，不受模型特定设计限制。
3. **稳定性提升**：物理约束的加入不仅降低了误差均值，还减小了多次运行的标准差，模型输出更鲁棒。
4. **定性结果**：图2显示PG模型预测的燃料密度图与真实值高度吻合，无明显非物理行为。

## 7. 优点

- **方法创新**：将物理约束作为可微软惩罚（而非硬约束）嵌入损失函数，避免传统PINN中PDE求解的梯度消失/爆炸问题，训练更稳定。
- **损失函数设计精细**：考虑了燃料不可再生性（Fuel Transport）、燃烧状态加权（burn/unburn）、火灾蔓延速度（ROS）等关键物理量，并通过可微近似处理了argmax的不可微问题。
- **跨架构验证**：同时在CNN-RNN（ConvLSTM）、Transformer（ViViT）、傅里叶神经算子（AFNONet）三种不同范式的模型上实验，证明方法通用。
- **计算效率**：模型为轻量级（单GPU，最多50 epochs），可实现快速推理，适合实时决策。
- **稳定性报告**：提供多次运行的均值和标准差，比单次结果更可靠。

## 8. 不足与局限

- **数据来源单一**：仅使用QUIC-Fire模拟数据，未在真实野火观测数据或物理实验数据上验证，模拟与真实之间的domain gap可能影响实际效果。
- **场景简化**：仅考虑平坦草地、恒定风速风向、单一燃料类型，未涉及地形坡度、植被多样性、风场变化等复杂因素。
- **预测窗口短**：输入6帧预测3帧（共约3秒），未评估更长序列（如数十帧）下的累计误差和物理一致性。
- **缺乏系统消融**：损失权重（λ）为启发式选取，未进行网格搜索或灵敏度分析；未单独分析各物理项的贡献度。
- **对比不充分**：未与基于PDE的PINNs、硬约束方法或其他已有的物理引导模型（如知识引导机器学习模型）进行定量比较。
- **计算资源细节缺失**：未报告总训练时间、内存占用等实际算力开销，不利于复现和公平比较。
- **可扩展性未验证**：仅测试了三种特定架构，未说明该框架是否适用于其他时空模型（如PredRNN、E3D-LSTM等）。

（完）
