---
title: Tikhonov-Regularized Physics-Informed Neural Networks for Terminal-State Distributed Optimal Control of Parabolic Partial Differential Equations
title_zh: 基于Tikhonov正则化的物理信息神经网络用于抛物型偏微分方程终端状态分布最优控制
authors: "Nguyen Thanh Quang, Ta Thi Thanh Mai"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24572v2"
tags: ["query:flow-pred"]
score: 9.0
evidence: 结合Tikhonov正则化的PINNs求解抛物型PDE最优控制，可应用于流场预测
tldr: 抛物线型PDE的终端状态跟踪最优控制是不适定问题，存在无穷多解。本文提出带Tikhonov正则化的PINNs框架，引导优化器寻找最小能量控制，恢复数值稳定性。理论证明了PINNs极小化器逼近连续正则化目标，并给出误差界。在热方程和Burgers方程上验证，正则化后的控制能量显著降低且轮廓更平滑。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24572v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1502, \"height\": 883, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24572v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1496, \"height\": 1351, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24572v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1233, \"height\": 526, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24572v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1498, \"height\": 971, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24572v2/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1499, \"height\": 1338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24572v2/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1229, \"height\": 527, \"label\": \"Figure\"}]"
motivation: 终端状态分布式最优控制的不适定性导致解不唯一，需要正则化以得到能量最小且稳定的控制。
method: 在PINNs损失中引入Tikhonov正则项，同时约束PDE残差、边界条件、终端匹配和能量惩罚。
result: 正则化PINNs在热方程和Burgers方程上准确达到目标终端态，控制能量远低于无正则化基线。
conclusion: Tikhonov正则化有效解决了PINNs求解终端控制问题的不适定性，提升了解的物理合理性与数值稳定性。
---

## 摘要
本研究提出了一种融合Tikhonov正则化的物理信息神经网络（PINNs）框架，用于求解受抛物型偏微分方程（PDE）约束的终端状态跟踪最优控制问题。该问题本质上是病态的，因为可能有无限多的分布控制都能使系统达到相同的期望状态，因此正则化引导优化器趋向于最小能量控制，恢复了数值稳定性，并产生了平滑且具有物理意义的解。在理论方面，我们建立了一致性结果，表明在残差和求积近似假设下，PINNs极小化器几乎达到了连续正则化目标；同时提出了一种新的误差估计，以PINNs训练残差和正则化参数为界限，限制了学习控制与最小能量解的偏差。在线性热方程和非线性Burgers方程上的数值实验表明，与未正则化的基线相比，正则化PINNs框架在准确达到目标终端状态的同时，产生的控制具有显著更低的能量和更平滑的轮廓。

## Abstract
In this study, we propose a Physics-Informed Neural Networks (PINNs) framework that incorporates Tikhonov regularization to solve terminal-state tracking optimal control constrained by parabolic partial differential equations (PDEs). This problem is inherently ill-posed, as infinitely many distributed controls may drive the system to the same desired state, so the regularization guides the optimizer toward the minimum-energy control, restoring numerical stability and yielding a smooth, physically meaningful solution. On the theoretical side, we establish a consistency result showing that PINNs minimizers nearly attain the continuous regularized objective under residual and quadrature approximation assumptions, and a novel error estimate that bounds the deviation of the learned control from the minimum-energy solution in terms of the PINNs training residuals and the regularization parameter. Numerical experiments on the linear heat equation and the nonlinear Burgers'equation demonstrate that the regularized PINNs framework accurately achieves the target terminal state while producing controls with significantly lower energy and smoother profiles compared to unregularized baselines.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：抛物型偏微分方程（PDE）的终端状态跟踪最优控制问题——寻找一个随空间和时间分布的控制器，使得系统在最终时刻的状态尽可能接近预设的目标状态。该问题本质上是病态的（ill-posed）：由于抛物型PDE的平滑性质，存在无穷多个不同的控制都能驱动系统达到同一个终端状态（非唯一性），违背了Hadamard适定性的唯一性条件。
- **研究动机**：传统基于网格和伴随的方法在高维或非线性问题中计算成本高昂且需专业领域知识；物理信息神经网络（PINNs）提供了无网格的替代方案，但直接用于终端控制问题时，不引入正则化会导致解不唯一、控制函数振荡、训练不稳定。因此需要一种机制来从无数可行解中选出最“合理”的一个——通常是最小能量、最平滑的控制。
- **研究意义**：通过引入Tikhonov正则化，将控制能量惩罚显式纳入PINNs损失，既保留了终端跟踪的原始目标，又恢复了问题的稳定性，使PINNs能够鲁棒地求解此类不适定问题。该方法可推广至热调控、流体力学、环境管理等工程领域。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将状态变量 \(u(x,t)\) 和控制变量 \(f(x,t)\) 分别用独立的神经网络参数化（全连接前馈网络，tanh激活函数）。
- 在PINNs的损失函数中显式加入H¹ Sobolev范数的Tikhonov正则项（对控制函数及其空间-时间梯度的L2惩罚），以引导优化器向最小能量控制收敛。
- 正则化项并非原始控制目标的一部分，而是在求解层面引入，仅作为数值稳定手段，不改变控制目标。

### 关键技术细节
1. **网络结构**：
   - 状态网络 \(u_\theta\) 和控制网络 \(f_\eta\) 均为全连接网络，隐藏层数和神经元数可调（实验中用4层×50神经元）。
2. **损失函数**（公式(8)）：
   \[
   \mathcal{L} = \mathcal{L}_{\text{int}} + \mathcal{L}_{\text{sb}} + \mathcal{L}_{\text{tb}} + w_J \mathcal{L}_{\text{target}} + \alpha \mathcal{L}_{\text{reg}}
   \]
   - \(\mathcal{L}_{\text{int}}\)：内部PDE残差平方和（用自动微分计算时间导数和空间导数）。
   - \(\mathcal{L}_{\text{sb}}\)：空间边界条件残差。
   - \(\mathcal{L}_{\text{tb}}\)：初始条件残差。
   - \(\mathcal{L}_{\text{target}}\)：终端状态与目标 \(u_d\) 的L2误差。
   - \(\mathcal{L}_{\text{reg}}\)：H¹ Tikhonov正则项，计算控制函数及其梯度的L2惩罚。
3. **优化器**：Adam，初始学习率1e-3，按预设epoch数量指数衰减（如3000/5000/10000处衰减10倍）。
4. **超参数选择**：
   - 正则化强度 \(\alpha\) 通过L-curve准则（绘制 \(\log \mathcal{L}_{\text{target}}\) vs \(\log \mathcal{L}_{\text{reg}}\)，选取曲率最大点）确定。
   - 终端损失权重 \(w_J\) 通过粗网格搜索选择，平衡PDE约束与终端跟踪。
5. **训练过程**：配置点（collocation points）定期重新采样以改善域覆盖。

## 3. 实验设计

- **数据集/场景**：
  - **线性热方程**：一维热方程（\(\Omega=(0,\pi), T=1\)），零Dirichlet边界条件，初始条件 \(u(x,0)=\sin x\)。目标终端状态为分段常数阶跃函数（在 \([\pi/3,2\pi/3]\) 上为1，其余为0）。
  - **非线性Burgers方程**：一维Burgers方程（\(\Omega=(0,1), T=1\)），粘性系数 \(\nu=0.1\)，零Dirichlet边界。目标终端状态为阶跃函数（在 \([0.3,0.7]\) 上为1，其余为0）。
- **基准（benchmark）**：两个问题的精确解析解已知（\(f\equiv 0\)时的前向解），用于验证网络架构。最优控制问题无解析解，论文将自身正则化结果与**未正则化版本（\(\alpha=0\)）** 对比，同时展示L-curve和网格搜索过程。
- **对比方法**：论文仅对比正则化（\(\alpha>0\)）与未正则化（\(\alpha=0\)）的PINNs方法，未与传统伴随法或其他数值优化方法进行比较。

## 4. 资源与算力

- **论文未明确说明**：未提及使用的GPU型号、数量、训练总时长（小时数）或特定计算平台。
- **仅提及的规模**：
  - 每个问题使用10,000个内部配置点，80个边界点，40个初始点，100个终端点和100个正则化点。
  - 热方程训练10,000个epoch，Burgers方程训练20,000个epoch。
  - 学习率在特定epoch衰减10倍（热方程在5,000 epoch，Burgers在10,000 epoch）。
  - 超参数搜索范围未详细列出，但提到了网格搜索 \(w_J\) 和L-curve计算多个 \(\alpha\) 值。

## 5. 实验数量与充分性

- **实验数量**：
  - 两个PDE案例（热方程和Burgers方程），每个案例包括：
    - 前向问题验证（验证架构能准确求解无控制时的PDE）。
    - 最优控制问题训练（正则化与未正则化对比）。
    - L-curve生成（多个 \(\alpha\) 值实验）。
    - 网格搜索 \(w_J\)（多个值实验）。
- **充分性评估**：
  - **正面**：覆盖了线性和非线性两类典型抛物型方程，目标函数采用不连续的阶跃函数（具有挑战性），验证了正则化在抑制震荡和降低能量方面的有效性。提供了损失收敛曲线、终端状态比较、控制轮廓、点态误差等可视化结果，实验展示较为全面。
  - **不足**：
    - 未执行多次随机初始化（不同种子）下的统计重复实验，结果可能受单次训练随机性影响。
    - 未与传统数值优化方法（如有限元+伴随法）进行定量性能对比。
    - 只测试了一维问题，未扩展到更高维度（如2D/3D）或更复杂PDE（如Navier-Stokes）。
    - 超参数搜索范围较粗略（仅粗网格搜索），可能未充分探索最佳组合。
    - 理论中的假设（如全局极小值、可达到目标）在实验中不完全满足（目标函数不可达），论文自己提到了这一点，但未设计验证理论边界的专门实验。

## 6. 论文的主要结论与发现

1. **正则化有效抑制不适定性**：在热方程和Burgers方程上，未正则化（\(\alpha=0\)）的控制函数在终端时刻附近出现大幅震荡（振幅超过±60），而正则化后的控制函数平滑且振幅显著降低（热方程约±6，Burgers约±15），证实H¹惩罚选择了最小能量解。
2. **终端跟踪精度不受损**：正则化PINNs能够准确达到目标终端状态，与未正则化版本相比，终端匹配误差（L2误差）相近或更优，说明正则化未牺牲控制目标。
3. **L-curve准则有效**：对两个问题均能给出合理的正则化参数（热方程\(\alpha^*=3.16\times10^{-3}\)，Burgers方程\(\alpha^*=5.62\times10^{-5}\)），Burgers方程需要更小的\(\alpha\)以允许必要的局部驱动力。
4. **理论结果**：
   - 命题4.1（一致性）：PINNs极小化器对应的目标函数值（终端损失+正则项）在残差较小时接近于连续正则化最优值。
   - 定理4.7（误差界）：对于线性抛物问题，在源条件假设下，学习控制与最小能量控制的H¹范数误差有上界 \(O(\epsilon^{1/4}/\sqrt{\alpha} + \alpha)\)；当 \(\alpha \propto \epsilon^{1/6}\) 时达到 \(O(\epsilon^{1/6})\)。
   - 推论4.10：状态近似也有类似收敛性。
5. **非线性情况的扩展**：命题4.11给出了非线性情况下同结构误差的启发式说明，数值结果支持其合理性。

## 7. 优点

- **理论分析扎实**：给出了严格的一致性结果和误差界（对线性情形），且误差界与训练残差和正则化参数直接关联，为方法提供了理论基础，这在PINNs应用于最优控制的文献中较为少见。
- **方法设计简洁有效**：将Tikhonov正则化自然融入PINNs损失，无需改变网络结构或训练流程，可直接利用现有的PINNs框架实现。
- **自动参数选择**：采用L-curve准则自动选择正则化参数，避免了手动调参的主观性，且在两个问题上都给出了合理结果。
- **实验验证充分**：线性和非线性两个典型问题，均展示了正则化前后的显著差异，并给出了可视化控制曲面、点态误差图等，直观性强。
- **问题陈述清晰**：明确指出了不适定性的数学根源（控制到终端状态映射的核非平凡），并对连续和离散情况下的凸性/非凸性做了仔细区分，有助于理解方法本质。

## 8. 不足与局限

- **理论假设强**：定理4.7需要网络的全局最小化器、源条件（最小能量控制在某算子值域内）、目标可达性等假设。这些假设在一般应用中很难严格满足（如论文实验中的阶跃目标即不可达），因此理论误差界在实际中只具有指导意义，不能直接应用。
- **实验规模小**：仅测试了一维、低非线性程度的问题，未拓展至二维/三维、对流占优、混沌系统等更具挑战性的场景。
- **缺少与传统方法的比较**：未与有限元+伴随法、投影梯度法等经典最优控制解法进行性能和效率对比，难以评估PINNs方法相对于成熟数值方法的实际优势。
- **缺乏统计稳健性分析**：未报告多次训练的均值和方差，无法判断结果对随机初始化、采样点扰动等的敏感性。
- **计算资源未报告**：读者无法复现或评估成本；训练epoch数量相对较多，可能对高维问题不可扩展。
- **未考虑控制约束**：实际应用中常存在控制幅值或能量上限，论文未讨论如何在该框架下处理约束。
- **网络架构选择不敏感分析**：仅使用了一种网络结构（4×50），未讨论层数、宽度、激活函数对结果的影响。

（完）
