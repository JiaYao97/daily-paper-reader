Title: Self-consistent orbital-free nuclear density functional theory with a physics-constrained learned nonlocal kinetic energy functional

URL Source: https://arxiv.org/pdf/2607.23328v1

Published Time: Tue, 28 Jul 2026 00:43:37 GMT

Number of Pages: 15

Markdown Content:
# Self-consistent orbital-free nuclear density functional theory with a physics-constrained learned nonlocal kinetic energy functional 

Fumihiro Imoto 

(Dated: July 2026) Nonlocal kinetic-energy density functionals (KEDFs) can encode nuclear shell structure in orbital-free density functional theory (OFDFT), but self-consistency requires accurate functional derivatives and a stable solution of the Euler equation. We construct a density-dependent-kernel KEDF whose correction is learned from Kohn–Sham (KS) reference data. Linearity in the kernel shape yields analytic Euler–Lagrange (EL) responses. The fit uses exact energy-matching equalities and soft quadratic inequality penalties for violations of prescribed tail-response and selected-path energy-rise margins, evaluated by an active-set repeated penalized least-squares iteration. We formulate the radial EL equation as the rearranged one-orbital eigenproblem. In a constant-kF 16 O benchmark it converges without density mixing and agrees with imaginary-time evolution (ITE) to sub-keV energy. Adaptive-step ITE gives final species EL residuals of at most 0 .07 MeV in the reported calculations, and rearranged diagonalization reaches the same stationary densities. For spherical N = Z systems without spin–orbit or Coulomb terms, nucleus-specific fits for A = 16–140 reproduce shell patterns and radii. A correction trained on three nuclei transfers the shell pattern and radius, but not the absolute energy, to a previously unseen A = 140 system. Preliminary fits without the energy-rise rows collapse to centrally depleted profiles; including those rows removes the instability at the fit stage, while a Thomas–Fermi-limit-preserving spatial-gradient term, implemented and released, neither repairs the preliminary collapse nor is needed by the final functional. A Gaussian-process formulation provides finite-grid hyperparameter selection and conditional parameter-uncertainty bands. These results establish self-consistent nonlocal nuclear OFDFT in this benchmark and quantify its numerical and transferability limits. 

I. INTRODUCTION 

Nuclear density functional theory in its Kohn–Sham (KS) form [1–3] is the standard microscopic framework for medium-mass and heavy nuclei, but its cost is dom-inated by the computation of single-particle orbitals. Orbital-free density functional theory (OFDFT), which expresses the kinetic energy directly as a functional of the density, offers in principle a dramatically cheaper al-ternative and has a long history in electronic-structure theory [4, 5]. In nuclear physics, however, Thomas– Fermi–type and semiclassical functionals fail to repro-duce shell effects, and this failure was long taken as evi-dence that orbital-free approaches are intrinsically blind to shell structure, a view challenged only recently by data-driven functionals [6, 17] Recently, Wu, Col` o, Hagino, and Zhao [7] demon-strated that a nonlocal KEDF, built from the Lindhard response of nuclear matter in close analogy with elec-tronic nonlocal functionals [8–12], does encode shell ef-fects: evaluated on KS densities, it reproduces the nu-cleon localization function (NLF), an established indi-cator of shell structure. That work evaluated the func-tional on given densities; self-consistent orbital-free cal-culations, in which the density itself is obtained by mini-mizing the nonlocal functional, were left as an open prob-lem. The difficulty is generic and well documented in electronic ML-KEDF studies: a functional accurate in 

values on reference densities may still possess an inac-curate functional derivative , so that its variational min-imum drifts away from the physical density [13–16]. In nuclear physics, kernel-regression KEDFs trained on den-sity values have been explored [17], with self-consistency stabilized by projection techniques, but without nonlocal shell structure. In this work we demonstrate that self-consistent orbital-free calculations with a shell-structured nonlo-cal KEDF are possible, by combining three ingredients. (i) An implementation of a density-dependent-kernel (dd) nonlocal functional with an analytic derivative of the rep-resented finite-table nonlocal term, in which the Euler– Lagrange (EL) potential includes all kernel density de-pendencies. (ii) Functional-derivative-informed fitting :because the functional is exactly linear in the dimen-sionless kernel shape g, the EL potential generated by any kernel-shape basis function can be computed ana-lytically without finite-difference error; the learned ker-nel correction δg is then fit simultaneously to NLF val-ues, the analytic per-basis Euler–Lagrange response of the represented functional, and total energies. This is the functional-space analogue of Sobolev training [15]. (iii) Quadratic inequality penalties that discourage be-havior invisible to value fits: loss of positivity of the re-sponse ratio in the far tail and energy decrease along prescribed density changes that had produced central-density or tail artifacts in preliminary self-consistent cal-culations. Here “functional derivative” refers to variation with respect to the density; it is distinct from the spa-tial density gradient ∇ρq introduced later in the gradient regularization term. We work in the benchmark setting of Ref. [7]: N = Z

systems, a simplified Skyrme (SkP-based [18]) interac-tion without spin-orbit and Coulomb terms, and spher-ical symmetry. Without spin-orbit coupling the magic arXiv:2607.23328v1 [physics.comp-ph] 25 Jul 2026 2numbers follow the oscillator sequence 2 , 8, 20 , 40 , 70 , . . . ,so that A = 16 , 40 , 80 , 140 are consecutive doubly-magic N = Z systems, allowing a clean study from light to heavy nuclei. Within this setting we show: (a) per-nucleus learned functionals whose self-consistent densities closely track the KS density and full NLF shell structure, with the remaining central-density de-viations quantified; (b) a single learned kernel correc-tion, trained on three nuclei with A ≤ 80, whose fi-nal self-consistent densities remain free of the identi-fied central-depletion and tail artifacts for all train-ing nuclei and predict the NLF shell structure and radius of the previously unseen A = 140 system— a central-depletion instability, exposed by preliminary fits and removed at the fit stage by the selected-path energy-rise penalty; (c) a Gaussian-process reformula-tion providing uncertainty quantification; and (d) arearranged-diagonalization formulation whose constant-

kF 16 O benchmark reaches the same discrete solution as a prolonged ITE trajectory. We use the descriptive phrase “physics-constrained learned functional” through-out. Mathematically the scheme is kernel-based super-vised regression with analytic functional-derivative su-pervision. Each fixed-active-set least-squares subprob-lem is convex, the energy-matching equalities are exact, and the two inequality-margin terms are soft penalties rather than hard constraints.   

> II. FRAMEWORK A. Kohn–Sham reference

The reference model is the simplified Skyrme en-ergy density functional of Ref. [7], based on the SkP parametrization [18] with spin-orbit and Coulomb terms omitted and N = Z. Spherical KS equations are solved on a radial grid ( nr = 500; box Rbox = 20 fm and 

ℓmax = 5–6 for 16 O and 40 Ca, Rbox = 22 fm and 

ℓmax = 12 for A = 80 and A = 140) to a self-consistency residual below 10 −7 fm −3/2. Specifically, the specieswise density-map residual is 

RKS ,q =

 1

Nq

Z

d3r (ρout ,q − ρin ,q )2

1/2

. (1) The released nr = 250 initial densities used by the OFDFT calculations are the corresponding every-second-point samples of these KS density profiles. For each nu-cleus the KS calculation yields the density ρKS ,q (r) for species q, the corresponding KS kinetic density τKS ,q (r), and the reference energy EKS . Every density in this work is a number density: ρq has units fm −3 and is normal-ized as 4 π R Rbox  

> 0

r2ρq (r) dr = Nq . Because all systems considered here have N = Z and identical neutron and proton profiles, we often write ρKS for either species and use ρtot = ρn + ρp = 2 ρq for the total nucleon density. Here Nn = N , Np = Z, and A = N + Z.Shell structure is monitored through the species-resolved nucleon localization function. Suppressing the species label q only in the following display, we define the dimensionless response ratio 

χ(r) = τ (r) − τvW (r)

τTF (r) , NLF( r) = 11 + χ(r)2 , (2) Here τ ≡ τq is the kinetic density and has units fm −5;the corresponding physical kinetic-energy density is Cτ q ,with C = ℏ2/(2 m) and m the common nucleon mass used in this N = Z benchmark. τTF = cTF ρ5/3 

> q

, cTF = 

> 35

(3 π2)2/3, and τvW = |∇ √ρq |2 is the von Weizs¨ acker ki-netic density. Thus χ compares the part of τq beyond the von Weizs¨ acker term with the local Thomas–Fermi scale, while NLF ∈ [0 , 1] is its bounded localization di-agnostic. The KS NLF exhibits the characteristic shell oscillations (central dips or peaks and surface maxima) that the tested local and semilocal approximations do not reproduce. Two caveats apply throughout: the NLF is even in χ, so NLF agreement is equivalent to χ agreement only where χ ≥ 0 (our fits therefore match χ itself, and small negative-χ excursions occur only in the far tail); and since the local kinetic-energy density of a nonlocal functional is not uniquely defined point by point (it is gauge dependent), the NLF is used as a diagnostic in the specific convention defined by Eq. (3).  

> B. Nonlocal density-dependent-kernel functional

For either species q ∈ { n, p }, the KEDF kinetic den-sity used to define the NLF is written as the following functional of that species density: 

τ KEDF  

> q

[ρq ]( r) = τvW (r) + cNL ρ5/6 

> q

(r)

Z

d3r′ t(r, r′)

× g t1/3|r − r′|ρ5/6 

> q

(r′)

− cCORR ρ5/3 

> q

(r), (3) The unprimed coordinate r is the observation point and 

r′ is integrated over all space. The symmetric pair-density variable t(r, r′) = 3 π2 [ρq (r) + ρq (r′)] /2 has units fm −3 and is the cube of the symmetrized local Fermi momentum, kF (r, r′) = t1/3 (a heuristic pair-density dependence, not a re-derivation from the Lindhard re-sponse of inhomogeneous matter), cNL = 96 125 (3 π2)2/3,

cCORR = 21 125 (3 π2)2/3 are dimensionless constants, and 

g(y) is a dimensionless radial kernel shape normalized as R d3y g (y) = 4 π R ∞ 

> 0

y2g(y) dy = 1 analytically, con-structed from the Lindhard function as in Ref. [7]. Here 

q ∈ { n, p } labels the neutron or proton species, ρq is the corresponding number density with R ρq d3r = Nq ,and τ KEDF  

> q

is its KEDF kinetic density; the dimension-less kernel argument is y = kF R with R = |r − r′|, and the subscripts abbreviate the nonlocal kernel term (NL) and the local Thomas–Fermi correction (CORR). The 3latter is a subtractive local term chosen so that the ana-lytic base functional recovers the uniform Thomas–Fermi limit. In the numerical representation the momentum transform is truncated and the resulting kernel is tabu-lated on a finite y interval. This finite transform/table representation has a cutoff-dependent numerical normal-ization error and is not renormalized automatically in the present implementation. The local correction cCORR ρ5/3

> q

therefore restores the Thomas–Fermi limit of the ana-lytic (infinite-table) base functional, whereas a finite ta-ble retains a small cutoff-dependent error. The fitted shift s of this local correction is fixed by finite-nucleus energy equalities and does not impose an additional uniform-matter condition. Consequently, the finite-table norm depends on the momentum cutoff and table ex-tent and is not a universal constant of the implemen-tation. The kinetic energy is TKEDF = C R d3r τ KEDF tot ,where C ≡ ℏ2/(2 m) = 20 .73553 MeV fm 2 and τ KEDF tot =

τ KEDF  

> n

+ τ KEDF  

> p

. The gradient regularization introduced in Eq. (29) is a separate energy contribution, so the full kinetic part is T = TKEDF +E∇ (with E∇ = 0 for the base and per-nucleus functionals). The total OFDFT energy is E = T + ESkP int [ρtot ] in the present benchmark, where 

ESkP int denotes the interaction part of the simplified SkP functional rather than the complete KS energy. For clarity, the KEDF part is species diagonal, 

TKEDF [ρn, ρ p] = X

> q=n,p

T KEDF  

> q

[ρq ], ρtot = ρn + ρp.

(4) In the present N = Z setting, ρn = ρp = ρq = ρtot /2and TKEDF = 2 T KEDF  

> q

, with T KEDF  

> q

= C R d3r τ KEDF  

> q

(r). The nonlocal contribution for one species can be written as the ordered double integral 

TNL ,q [ρq ] = Cc NL 

Z

d3r d 3r′ ρaq (r)ρaq (r′)

× f1(R; t), C ≡ ℏ2

2m , a = 56 , (5) The exponent a = 5 /6 is the power of each explicit den-sity prefactor, and C converts the integrated kinetic den-sity to energy. The pair ( r, r′) is counted in both orders; there is no additional symmetry factor because Eq. (5) is obtained by directly integrating the local density in Eq. (3). The dimensional pair kernel is defined by 

f1(R; t) = t g (y), y = t1/3R = kF R, t = k3 

> F

= 3π2

2 [ρq (r) + ρq (r′)] , R = |r − r′|.

(6) Here f1 is the dimensional pair kernel: since t has units fm −3 and g is dimensionless, f1 has units fm −3. The exact scaling in Eq. (6) gives 

∂f 1(R; t)

∂t = g(y) + y

3 g′(y), g′(y) ≡ dg dy . (7) Here the partial derivative holds the separation R fixed. Introducing the two density-dependent nonlocal integrals 

Iq (r) = 

Z

d3r′ ρaq (r′)f1(R; t),

Jq (r) = 

Z

d3r′ ρaq (r′)

h

g(y) + y

3 g′(y)

i

, (8) separates the variation of the explicit density powers ( Iq )from the variation of the density-dependent pair scale (Jq ). Their units are fm −5/2 and fm 1/2, respectively. They are not translation-invariant convolutions because 

t depends on both density values. The nonlocal Euler– Lagrange (EL) potential is then 

vNL ,q (r) ≡ δT NL ,q 

δρ q (r)= Cc NL 

 53 ρ−1/6 

> q

(r)Iq (r)+3 π2ρ5/6 

> q

(r)Jq (r)

i

≡ vdir ,q (r) + vrearr ,q (r). (9) Both vNL ,q and the other v’s below have units of energy. The labels dir and rearr denote, respectively, the direct variation of the two explicit density prefactors and the rearrangement term generated by the density dependence of t and hence of kF . The coefficient 5 /3 = 2(5 /6) comes from varying the two explicit density prefactors in the symmetric integrand. The coefficient 3 π2 = 2(3 π2/2) similarly combines the density dependence of t at the two ends of each pair. Including the von Weizs¨ acker and local correction contributions, the full kinetic potential is 

vT,q (r) ≡ δT q

δρ q (r)= −C ∇2√ρq

√ρq

+ vNL ,q (r)

− C 53 ¯cCORR ρ2/3 

> q

(r) + v∇,q (r). (10) Here ¯ cCORR = cCORR + s, with the dimensionless shift 

s = 0 for the base functional. The corresponding lo-cal contribution is Tcorr ,q = −C¯cCORR 

R d3r ρ 5/3 

> q

and 

vcorr ,q = −(5 C/ 3)¯ cCORR ρ2/3 

> q

. The term v∇,q [Eq. (30)] is the gradient-regularization potential, which vanishes for s2 = 0 (the base and per-nucleus functionals) and is nonzero only for the shared multi-nucleus functional used in the final transfer test. Stationarity at fixed neu-tron and proton particle numbers follows from δ{E −P 

> q

μq

R d3r ρ q }/δρ q = 0 and gives the specieswise EL equation 

vT,q (r) + δE SkP int 

δρ tot (r) = μq . (11) Here μq is the Lagrange multiplier (chemical potential) for Nq . The Skyrme interaction depends on ρtot , so 4its species derivative is written as δE SkP int /δρ tot . Vary-ing the neutron and proton densities independently gives Eq. (11). If the N = Z restriction is imposed be-fore the variation, both the total functional and the particle-number constraint acquire the same factor of two, which cancels. Equations (7)–(9) are the exact functional derivative of the represented finite-table non-local term: the implementation evaluates both g and g′

from the same spline. (The von Weizs¨ acker and Skyrme-gradient terms are discretized with central first differ-ences; the ITE flow is thus not the exact discrete gra-dient flow of that first-difference energy—Appendix B.) The analytic derivatives are verified against central finite differences to a relative accuracy of ∼ 10 −11 at optimal step (Appendix B). Self-consistent solutions are discussed in Sec. II C.  

> C. Self-consistent solution strategy

Completing self-consistent calculations with a nonlo-cal functional of the form (3) requires a stable realiza-tion of its analytic EL potential. We first formulate the radial EL problem through the kinetic rearrange-ment [19]; it gives an iteration in terms of one auxiliary eigenstate. A density left unchanged by this update is called a fixed point. Its quantitative validation below is restricted to the base constant-kF 16 O functional. The learned density-dependent results use the common pro-duction numerical settings described at the end of this subsection and in Appendix A. In the benchmark only, the pair kernel is held independent of the density, 

f (0) 1 (R) = k3

> F, 0

g(kF, 0R), kF, 0 = 0 .80 fm −1. (12) Thus the explicit ρ5/6 

> q

factors remain density dependent, but the pair scale does not: Jq = 0 and the kernel-density rearrangement term in Eq. (9) is absent. The algebraic “rearrangement” used by the eigensolver be-low is a separate concept. We use nμ for the number of Gauss–Legendre nodes for μang = cos θ in the angu-lar part of the nonlocal integral; it is unrelated to the chemical potential μq .

Rearranged-diagonalization formulation. Choose apositive dimensionless rearrangement parameter λ and write the kinetic functional as T = λT vW + T (λ) 

> r

, where 

T (λ) 

> r

= T − λT vW is the remainder treated as a density-dependent local potential that is held fixed while each auxiliary eigenproblem is solved. For the present decom-position, T = TvW +TNL +Tcorr +T∇, with T∇ ≡ E∇, this implies δT (λ) 

> r

/δρ q = (1 − λ)vvW ,q + vNL ,q + vcorr ,q + v∇,q 

(with v∇,q = 0 for s2 = 0). Collecting all terms other than the explicitly propagated λT vW contribution, define 

vrest ,q = vNL ,q + vcorr ,q + v∇,q + δE SkP int 

δρ tot 

. (13) Dividing the rearranged EL equation by λ and setting 

uq (r) = rpρq (r), with units fm −1/2, removes the first radial derivative from the spherical Laplacian. With 

vvW ,q = −C∇2√ρq /√ρq , the resulting one-dimensional auxiliary eigenproblem is (all potentials are evaluated at the current neutron and proton densities) 

− C d2uq (r)

dr 2 + 1 − λλ vvW ,q (r)uq (r)+ 1

λ vrest ,q (r)uq (r) = μq

λ uq (r), (14) with uq (0) = uq (Rbox ) = 0 and 4 π R Rbox  

> 0

|uq (r)|2 dr =

Nq . The eigenvalue on the right-hand side is the scaled chemical potential μq /λ . At a fixed point the rearranged terms cancel algebraically, so the continuum solution is independent of λ. Numerically, the cancellation holds up to the stated discretization and tail safeguards. At each density-update iteration k we diagonalize the tridiago-nal frozen radial Hamiltonian, use its normalized lowest eigenstate to define ρ(k)0,q (r) = |u(k)0,q (r)/r |2, and optionally mix the input and output densities. Suppressing q in the iteration formula, 

ρ(k+1) = (1 − αmix )ρ(k) + αmix ρ(k)0 . (15) Here the subscript 0 denotes the lowest auxiliary eigen-state and the superscript ( k) denotes the density-update iteration. The regular r → 0 limit is used when recon-structing ρ0,q . The density-mixing fraction αmix ∈ (0 , 1] gives the unmixed map at αmix = 1; smaller values under-relax it. This is a one-density-orbital EL solver, not a many-orbital KS diagonalization. Convergence is moni-tored with the weighted density-map residual 

R(k) 

> ρ

=

Z 

d3r  ρ(k)0 − ρ(k)2

1/2

. (16) for each species (identical in the present N = Z cal-culations). Because the measure is d3r = 4 πr 2dr and the integrand is a squared number density, Rρ has units fm −3/2.The implementation shares the convolution, analytic potential, and energy evaluator with ITE; the com-parison therefore tests the nonlinear density-update al-gorithm rather than independently reimplementing the functional. The frozen auxiliary spectrum is a useful but incom-plete convergence diagnostic. Let δρ (k) denote the devi-ation of the kth input density from a fixed-point density. Linearizing the density map gives, schematically, 

δρ (k+1) = [(1 − αmix )I + αmix Mλ] δρ (k),

Mλ = RλKλ, (17) where I is the identity map, Mλ is the Jacobian of the unmixed density map, and Rλ is the response of the nor-malized lowest-eigenstate density to a perturbation of the frozen potential. The potential-feedback part is 

Kλ = (1 − λ)KvW + Krest 

λ (18) 5                                

> TABLE I. Rearranged diagonalization for the base constant-
> kF16 O benchmark ( nr= 250, kF= 0 .80 fm −1,nμ= 24). It-eration counts use Rρ<10 −8fm −3/2. The gap is between the two lowest eigenvalues of the final frozen auxiliary Hamilto-nian. The dash denotes no convergence within 500 iterations.
> λαmix iterations E[MeV] ∆ϵaux [MeV] 11.0–––10.350 −117 .1351 11 .365 31.041 −117 .1351 10 .852 10 1.0136 −117 .1351 10 .411

where KvW = δv vW /δρ and Krest = δv rest /δρ are functional Hessian kernels evaluated at the fixed point; species indices and radial integration arguments are sup-pressed. First-order eigenvector perturbation theory in-troduces inverse auxiliary level separations into Rλ, but local convergence is controlled by the spectral radius (the largest absolute eigenvalue) of the full mixed map in Eq. (17), not by one gap alone. Table I reports the constant-kF 16 O scan. The naive (λ, α mix ) = (1 , 1) map does not converge within 500 it-erations. Mixing at λ = 1, or rearrangement with λ = 3 or 10, restores convergence; λ = 3 requires the fewest density-update iterations among the tested settings: 41 without mixing. For this run Rρ < 10 −8 fm −3/2, and the late-iteration geometric mean of R(k+1)  

> ρ

/R (k) 

> ρ

over itera-tions 20–40 is approximately 0 .71. The converged frozen gap is 10 .852 MeV. This is the gap of the auxiliary one-density-orbital Hamiltonian, not a physical many-orbital nuclear shell gap. Moreover, the converged λ = 1 mixed case has a larger auxiliary gap, 11 .365 MeV, so the scan does not support a gap-only explanation of stability. For ( λ, α mix ) = (3 , 1) the diagonalization energy is 

−117 .1351065 MeV. The energy after the fixed budget of 8000 ITE steps is higher by 41 .616 keV. Extending the same ITE trajectory to 20000 steps reduces the en-ergy difference to 0 .368 keV and the relative radial L2

density difference, ∥ρITE − ρdiag ∥2/∥ρdiag ∥2 with the d3r

measure, to 4 .59 × 10 −4. The independently monitored EL residual at the converged diagonalization density is 0.048 MeV; this nonzero value is consistent with the dif-ferent radial operators used by the map and by that di-agnostic (Appendix B). Thus the comparison establishes agreement of two density-update algorithms for the re-leased discretization, not an exact zero-residual solution. 

Production ITE calculation. All learned density-dependent results below are obtained from the KS den-sities with the split-step ITE of Appendix A using an adaptive step-size schedule: the step begins at ∆ t =10 −5 MeV −1 and is grown over intervals of decreasing monitored energy and shrunk (with a return to the last accepted state, hereafter rollback) whenever the moni-tored energy would increase, up to the maximum step ∆tmax = 5 × 10 −5 MeV −1. With rollback the moni-tored energy is non-increasing at the accepted monitor points by construction. We assess stationarity with the species amplitude ϕq = √ρq , the frozen EL Hamiltonian, its Rayleigh quotient, and the residual ˆhq = −C∇2 + vloc ,q ,μq =

R d3r ϕ q ˆhq ϕq

R d3r ϕ 2

> q

,REL ,q =

"

4π

Z Rbox 

> 0

dr r 2 ˆhq ϕq − μq ϕq

> 2

#1/2

. (19) Here vloc ,q = vNL ,q + vcorr ,q + v∇,q + δE SkP int /δρ tot con-tains every multiplicative term, while −C∇2 is applied as the kinetic operator. Because R ϕ2 

> q

d3r = Nq , the de-nominator in the Rayleigh quotient is Nq . The reported 

REL ,q is the unnormalized, specieswise norm; it has units MeV and retains an approximate pNq size scaling. Afixed small step relaxes the light nuclei fully but leaves the heavy systems short of stationarity within the itera-tion budget; the adaptive schedule reaches REL ,q = 0 .02– 0.05 MeV for all systems (Table II) at the same bud-get, and the rearranged diagonalization independently reaches the same fixed points: for the universal functional the final ITE and diagonalization densities agree to a rel-ative radial L2 density difference of 2 .2×10 −5, 7 .9×10 −5,1.9×10 −4 and 2 .9×10 −4 for 16 O, 40 Ca, A=80 and A=140, with identical central densities to four digits. These fixed points are not artifacts of the KS starting profiles: a diag-onalization run started from a Woods–Saxon density un-related to the KS solution relaxes to the same 16 O density (relative L2 difference 1 × 10 −7). For A=80 the Woods– Saxon start likewise gives a centrally filled solution, but not identically the KS-initialized one ( ρc = 0 .197 ver-sus 0 .183, relative L2 difference 1 .5 × 10 −2): as with the angular quadrature (Appendix B), the A=80 cen-tral density carries a percent-level solver-path sensitivity that the lighter systems do not. The split-step algorithm, residuals, and this initialization test are documented in Appendix A.   

> III. LEARNING THE KERNEL CORRECTION A. Parametrization and analytic functional-derivative data

The base functional of Eq. (3) reproduces the KS NLF only qualitatively. We therefore learn a correction to the kernel shape, 

g(y) → g(y) + δg (y), δg (y) = X

> b

cb ˜γb(y), (20) together with a fitted shift of the local correction, 

cCORR → cCORR + s. The basis contains B = 14 dis-placed Gaussians in y. Each is Thomas–Fermi neutral-ized according to 4π

Z ∞

> 0

dy y 2eγb(y) = 0 , (21) 6so it does not change the uniform-density contribution when s is held fixed. The integer b = 1 , . . . , B labels a basis function, the amplitudes cb and the shift s are di-mensionless fit parameters, and δg is dimensionless like g.Because Eq. (3) is exactly linear in g, three consequences follow. First, the kernel correction inherits the full varia-tional machinery with no new derivative code: Eqs. (7)– (9) remain valid under g → g + δg and g′ → g′ + δg ′,while Eq. (10) uses ¯ cCORR = cCORR + s. Second, define the complete EL potential vEL ,q = vT,q + δE SkP int /δρ tot .On a fixed KS density its coefficient responses are 

vb(r) = ∂v EL ,q (r)

∂c b

,vs(r) = ∂v EL ,q (r)

∂s = − 5C

3 ρ2/3 

> q

(r), (22) and have units MeV. They are obtained from analytic EL evaluations, with no finite-difference approximation to the functional derivative. Third, all responses used below are affine in the coefficients, so each fixed-active-set least-squares subproblem is convex.  

> B. Components of the fitting objective

Collect the kernel coefficients and local-correction shift in θ = ( c1, . . . , c B , s )T. They are determined from three least-squares data groups, one exact energy-matching equality per training nucleus, and two soft inequality-margin penalties, all evaluated on fixed KS density pro-files. These groups are called “blocks” in the numerical implementation. 1. NLF-value data : the response ratio χ of Eq. (2), which is linear in δg , is matched to χKS with a weight containing the absolute NLF sensitivity 

SNLF = |d NLF /dχ | = 2 |χKS |/(1 + χ2KS )2 and an additional radial weight that emphasizes the nu-clear interior. The renamed symbol SNLF avoids confusion with the nonlocal integral Jq .2. EL-potential data (functional-derivative or Sobolev term): the complete model EL potential is matched to a constant chemical potential on the KS density, 

vbase (r) + P 

> b

cbvb(r) + s v s(r) ≈ μ, using the an-alytic per-basis potentials vb. Here vbase contains the von Weizs¨ acker, uncorrected density-dependent kernel, local correction, and simplified SkP interac-tion potentials at ρKS ; μ is a per-nucleus auxiliary chemical potential in MeV. 3. Exact energy-matching equality : let τdd be the base density-dependent-kernel kinetic density on 

ρKS and let ∆ τ (θ) be the fitted correction, so that 

τθ = τdd + ∆ τ . The equality 

C

Z

d3r ∆τ (θ) = C

Z

d3r [τKS − τdd ] (23) is imposed once per training nucleus. Thus the cor-rected kinetic energy on the fixed KS density equals the KS kinetic energy; because the interaction func-tional sees the same ρKS , the corrected total energy there is EKS . The factor C may be cancelled, so the released linear system stores this equality in inte-grated kinetic-density units. A bordered Karush– Kuhn–Tucker (KKT) system imposes it exactly. 4. Surface–tail data : χ is matched in a surface–tail band ( r ∈ [4 .5, 8] fm for light, [5 .5, 9] fm for heavy nuclei) with a weight that limits the influence of the exponentially growing χKS .5. Tail-margin penalty : on 8 ≤ r < 11 .5 fm for light nuclei and 9 ≤ r < 13 fm for heavy nuclei, a quadratic penalty discourages χ < χ min , with the target margin χmin = 5. It prevents spurious zero crossings of χ, which would appear as artifi-cial NLF = 1 peaks at ρ ≲ 10 −7 fm −3. The oscilla-tory long-range response is inherited from the sharp Fermi surface of the Lindhard kernel; the penalty moves its sign changes outside the stated diagnostic interval. 6. Selected-path energy-rise penalty : trial densities are discrete points ρνj = ρKS ,ν + ξj δρ νj with ξj > 0, chosen to remain nonnegative and to conserve the particle number. They follow density changes that produced central-density artifacts in preliminary self-consistent calculations. A second quadratic penalty discourages ∆ Eνj < ε , where ε = 2 MeV and ∆ Eνj = Emodel [ρνj ] − Emodel [ρKS ,ν ]. These finite path-energy differences supplement the first-order EL-potential data; they should not be inter-preted as direct measurements of a local Hessian. The last two contributions can now be stated precisely. On the fixed input densities, both quantities are affine in the fit coefficients: 

χνi (θ) = χ(0)  

> νi

+ xT

> νi

θ,

∆Eνj (θ) = dνj + qT 

> νj

θ. (24) Here i labels a radial point in the tail set Hν , j la-bels a trial density, ν labels a training nucleus, and dνj 

is the base model’s path-energy change. With [ x]+ ≡

max( x, 0), the conceptual one-sided forms are 

QT (θ) = X

> ν

aT,ν 

X

> i∈H ν

[χmin − χνi (θ)] 2+,

QS (θ) = X

> ν,j

aS,νj [ε − ∆Eνj (θ)] 2+. (25) The positive tail weight aT,ν is dimensionless and aS,νj 

has units MeV −2. Each term is zero when its target margin is met and rises quadratically with the amount of violation. These are soft quadratic inequality penal-ties : they discourage but do not prohibit a violation. In 7contrast, Eq. (23) is a hard linear equality. There are no class labels and no classification loss. The implementation evaluates the soft penalties by an 

active-set repeated least-squares iteration . From the co-efficients at iteration k, it forms the sets of violated in-equalities 

A(k) 

> T,ν

= {i ∈ H ν : χνi (θ(k)) < χ min },

A(k) 

> S

= {(ν, j ) : ∆ Eνj (θ(k)) < ε }. (26) Only rows in these sets contribute quadratic residuals to the next solve. With the selected rows and their nor-malizations fixed, the resulting convex quadratic prob-lem is solved together with the exact energy equalities by the bordered KKT system; the active sets are then recomputed. We therefore refer to the procedure more fully as an active-set repeated penalized least-squares it-eration with exact linear equality constraints. Because the tail normalization may change with the active set, the released algorithm is a sequence of convex equality-constrained quadratic subproblems, not the minimization of one fixed global quadratic objective. The released GP code performs 16 updates; its active sets and coefficients cease to change at the 14th update for the released data. The finite-Gaussian-basis production fits use the same block structure and active-set machinery with 60 damped updates (damping 1 /2) and mild coefficient/shift ridge regularizers; they are released, with all settings fixed in the source, as kernel fit.py , which regenerates every per-nucleus and shared kernel from the released refer-ence data. The exact GP normalizations and weights are given in Appendix C. The Gaussian data terms admit an exact Gaussian-process interpretation: since those observations are linear functionals of δg , replacing the finite basis by a reduced-rank Gaussian-process prior gives GP regression in which the observations may be integrals or derivatives of the kernel rather than only its point values (operator obser-vations). The soft inequality penalties are added at the maximum-a-posteriori (MAP) stage. Hyperparameters are selected by a finite-grid marginal-likelihood compar-ison, and fixed-active-set inverse-Hessian bands are ob-tained for δg and fixed-density predictions (Sec. VI and Appendix C).  

> IV. RESULTS I: PER-NUCLEUS FUNCTIONAL FITTING

For the observables reported below, we use the total-density central value ρc ≡ ρtot (0) and the total-nucleon root-mean-square radius 

rrms ≡

 1

A

Z

d3r r 2ρtot (r)

1/2

, A = N + Z. (27) Thus ρc has units fm −3 and rrms has units fm. In the present N = Z calculations they can equivalently be eval-       

> FIG. 1. Value-only versus functional-derivative-informed learning for 16 O. (a) Total nucleon density ρtot : with a value-only fit (blue dashed) the ITE endpoint moves away from the KS density (black), whereas adding analytic per-basis EL-potential data and the exact energy equality (red) keeps it close to ρKS . (b) The species-resolved NLF at the correspond-ing endpoints; neutron and proton NLFs are identical here.

uated from either species density after dividing numer-ator and normalization by two. The NLF distance d is dimensionless and uses either species, whose neutron and proton profiles are identical here. We define it explicitly as ∆NLF (r) = NLF OF (r) − NLF KS (r),d2 =

Z RNLF 

> 0

dr w ρ(r)∆ 2NLF (r)

Z RNLF 

> 0

dr w ρ(r)

,wρ(r) = r2ρKS ,q (r), (28) with RNLF = 7 fm for 16 O and 40 Ca and 9 fm for the two heavier systems. The common angular factor 4 π cancels between numerator and denominator. Figure 1 isolates the central methodological point. A δg fitted to NLF values only reproduces the KS NLF essentially exactly on ρKS (d ≃ 10 −3), yet its self-consistent relaxation drifts by −2.2 MeV ( 16 O) and 

−16 .4 MeV ( 40 Ca), the central density overshoots by up to 30%, and the NLF structure at the final density de-grades ( d = 0 .05–0 .2). This is the nuclear manifesta-tion of the value/derivative dichotomy known from elec-tronic ML-KEDFs [13–15]. The weighted root-mean-square EL-fit mismatch is Rfit ,ν = [ P 

> i

wEL ,νi (vEL ,νi −

μν )2/ P 

> i

wEL ,νi ]1/2; it is a training-density diagnostic and is distinct from the self-consistent residual REL ,q in Eq. (19). Adding functional-derivative matching and the exact energy equality reduces Rfit ,ν on ρKS from 5 .7 to 0.4 MeV for 16 O and keeps the relaxation near ρKS : over the converged adaptive-step trajectory the energy moves by only 0 .018 MeV, and the final density reproduces ρc

and rrms of the KS solution to three digits. Figure 2 and Table II summarize the final per-nucleus functionals. The reevaluated endpoint energies lie be-low the KS values by 0 .4–6 .0 MeV (0 .3–0 .4%): the exact energy equalities are imposed at the fixed KS densities, and the subsequent self-consistent relaxation lowers the energy of the approximate functional below that con-8

TABLE II. Per-nucleus learned functionals: final adaptive-step ITE densities versus the KS reference. EKS is the indepen-dent KS total energy obtained with the same simplified SkP interaction and the same mass constant as the released solver (ℏ2/2m = 20 .73553 MeV fm 2; regenerated by the released script ks total energy.py ). E(64) OF ≡ E(64) OF [ρ(24) ITE ] is the OFDFT energy reevaluated with nμ = 64 on the final species density produced by the nμ = 24 ITE trajectory. The magnitude 

|∆E(24) ITE | = |E(24) end − E(24) 1 | uses the first and final monitored energies of that trajectory; accepted monitor energies are non-increasing. R(24) EL ,q is the final unnormalized specieswise residual of Eq. (19). The distance d is defined by Eq. (28). In entries 

x (xKS ), the first value is the final OFDFT result and the parenthesized value is the KS reference. Radii are in fm, energies and residuals in MeV, and central total densities in fm −3.

EKS E(64) OF |∆E(24) ITE | R(24) EL ,q [MeV] ρc (KS) rrms (KS) d

> 16

O −126 .50 −126 .89 0.02 0.017 0.146 (0.145) 2.741 (2.741) 0.009 

> 40

Ca −398 .89 −400 .54 0.34 0.054 0.188 (0.186) 3.402 (3.396) 0.014 

A=80 −895 .74 −898 .14 0.20 0.045 0.181 (0.177) 4.104 (4.092) 0.025 

A=140 −1677 .4 −1683 .4 4.1 0.049 0.203 (0.198) 4.811 (4.807) 0.027 

FIG. 2. Final per-nucleus functionals for 16 O and 40 Ca with EL-potential matching and the quadratic tail-margin penalty. Top: final total nucleon densities versus KS. Bottom: species-resolved NLF at the final ITE density; the blue dotted NLF curves omit the tail penalty and contain the artificial peak caused by a χ zero crossing near r = 6 .7 fm. 

strained point. For 16 O the adaptive-step ITE density reproduces the KS central NLF dip (0 .28 → 0.31), the surface peak (0 .87 → 0.87), and the tail decay to below NLF = 10 −3 at r = 7 .5 fm; the weighted NLF distance is 

d = 0 .009. For 40 Ca the double-hump structure is repro-duced (0 .48 /0.50 at the interior dip) with d = 0 .014. Two tail-specific ingredients are required: (i) the tail band, which suppresses an artificial oscillation of the learned 

χ around the exponentially growing χKS , and (ii) the one-sided tail penalty, which removes artificial NLF = 1 peaks caused by χ zero crossings; both artifacts live at 

ρ ≲ 10 −6 fm −3 and are invisible to density-weighted value fits. Applying the same fitting and relaxation procedure to the next two oscillator-magic systems (Fig. 3) shows that the procedure remains numerically stable across the sys-tems tested: the richer shell structure (three to four in-terior NLF extrema) is tracked across the interior region; radii agree to 0 .004–0 .012 fm, and the central densities 

FIG. 3. Application of the per-nucleus procedure to the next two doubly magic systems of the benchmark without spin– orbit coupling, A = 80 ( Z = N = 40) and A = 140 ( Z = N =70). Top: total nucleon density. Bottom: species-resolved NLF (identical for neutrons and protons), including its three to four interior extrema. 

agree to 2 .5% for both systems. The remaining system-atic degradation is also quantified: the monitored energy drift over the fixed 8000-step budget grows to 4 .1 MeV for A = 140 (0 .24% of E), while it stays at or below 0.34 MeV for the three lighter systems. 

V. RESULTS II: SHARED MULTI-NUCLEUS FIT AND TEST ON AN UNSEEN NUCLEUS 

To test transfer across nuclei we train a single (δg, s )on 16 O+ 40 Ca+ A=80 (per-nucleus data groups, three ex-act energy equalities, and per-nucleus chemical potentials as auxiliary parameters). All choices of functional form, basis size, relative data weights, hyperparameters, and selected-path energy-rise penalties were made using only these three training nuclei. No A = 140 observable or di-agnostic was inspected during model selection. Only af-ter the model and its coefficients had been fixed was A =9

FIG. 4. Shared multi-nucleus (“universal”) functional: a single kernel correction δg and local-correction shift, trained jointly on 16 O, 40 Ca and A = 80 with selected-path energy-rise penalties (built from the recorded central-depletion direction of a preliminary fit; text) and per-nucleus normalization of the tail-data weights; the production functional uses s2 = 0, i.e. no gradient regularization. Top: total nucleon density; bottom: species-resolved NLF. None of the four final densities exhibits the identified central-depletion or tail artifacts. The rightmost column, A = 140, is a holdout test: no A = 140 observable or diagnostic enters either fitting or model selection. TABLE III. Shared multi-nucleus functional (single δg and shift; trained on 16 O, 40 Ca, A = 80; no A = 140 observable or diagnostic enters fitting or model selection). Same conventions as Table II. E(64) OF is E(64) OF [ρ(24) ITE ], evaluated at nμ = 64 on the final nμ = 24 ITE density; the production shared functional uses s2 = 0, so no gradient-regularization energy enters. ∆EKS = E(64) OF − EKS . The labels “tr.” and “h.o.” denote a training nucleus and the holdout nucleus, respectively. The last column d is the density-weighted NLF distance to the KS reference, as in Table II; the parenthesized entries in the density and radius columns are again the KS reference values. The exact energy equalities hold at the fixed KS training densities; the subsequent self-consistent relaxation lowers each training energy by 1 .3–3 .2 MeV below EKS .

E(64) OF ∆EKS ρc (KS) rrms (KS) d

> 16

O tr. −127 .84 −1.3 0.162 (0.145) 2.774 (2.741) 0.096 

> 40

Ca tr. −401 .13 −2.2 0.172 (0.186) 3.404 (3.396) 0.097 

A=80 tr. −898 .91 −3.2 0.183 (0.177) 4.088 (4.092) 0.096 

A=140 h.o. −1719 .08 −41 .7 0.190 (0.198) 4.793 (4.807) 0.074 

140 evaluated; it is therefore a holdout, meaning a previ-ously unseen test nucleus. Using only the training nuclei, the fit design exposed a central-depletion failure whose diagnosis is itself instructive and whose resolution moti-vated the selected-path energy-rise rows of Sec. III; the preliminary no-path fit and all diagnostics below are re-leased and regenerable ( kernel fit.py shared nopath ;

results/preliminary correction/ ). (i) A preliminary shared fit without the energy-rise rows produced a qual-itative failure invisible at fit level (its fixed-density NLF distance for A = 80, 0 .082, is comparable to the re-leased shared fit’s fixed-density value): at the produc-tion quadrature ( nμ = 24) the KS-initialized A = 80 calculation collapses to an unphysical central depletion (ρc = 0 .061 versus 0 .177). (ii) Neither the gradient term of Eq. (29) (scanned up to s2 = 2 fm 4: ρc = 0 .061– 0.063) nor a finer angular quadrature ( nμ = 48: ρc =0.063) removes the depletion, which is consistent with an unfavorable-curvature direction of the fitted correc-tion itself that is not controlled by the first-order fit; the base functional is stable along the released trial paths (its path-energy changes are +1 .3 to +10 .2 MeV). (iii) The depletion is a genuine, algorithm-independent fixed point: relaxed from the KS density at the pro-duction setting ( nμ = 24, s2 = 0), the rearranged di-agonalization converges (residual below 10 −8) to a cen-trally depleted density, and the 8000-step imaginary-time endpoint agrees with it to a relative L2 density difference of 2 .0 × 10 −4, with identical central density (ρc = 0 .061 for both). Including the three energy-rise rows built from this recorded artifact direction (Sec. III; the endpoint density and the trial densities are released in 10 

scripts/path data/ ) removes the failure at its source: the released shared functional keeps the path energies near zero (∆ E = +0 .41, +0 .29, +1 .09 MeV at the three released trial densities—all uphill, though below the 2 MeV soft target), and its KS-initialized A = 80 relaxation is a stable, centrally filled profile already at 

s2 = 0 ( ρc = 0 .183 versus the KS value 0 .177; Table III). At the working nμ = 24 discretization the artifact is a localized central depletion ( r ≲ 2 fm, where the 2 s orbital contributes), while the surface is nearly unchanged. It can be regularized by supplementing the nonlocal kernel with a single gradient regularization term , added to the local correction of Eq. (3), 

E∇[ρn, ρ p] = s2

ℏ2

2m

X

> q=n,p

Z

d3r ρ 1/3 

> q

|∇ ρq |2, (29) with a single coefficient s2 of dimension fm 4. The di-mensions follow from ρ1/3 

> q

|∇ ρq |2d3r ∼ fm −6 and C ∼

MeV fm 2, so s2C converts the integral to energy. Its continuum functional derivative is 

v∇,q = s2

ℏ2

2m

h 13 ρ−2/3 

> q

|∇ ρq |2 − 2 ∇·  ρ1/3 

> q

∇ρq

i

, (30) where surface terms vanish for the stated Dirichlet/tail boundary conditions. The first term comes from differ-entiating ρ1/3 

> q

and the divergence term from integrat-ing the variation of ∇ρq by parts. The potential v∇,q 

is included in the full kinetic potential vT,q , the rear-ranged auxiliary potential vrest ,q , and the ITE local po-tential vloc . In the code the potential is not this con-tinuum expression but the exact discrete adjoint of the first-difference energy (Appendix B), so it matches fi-nite differences of the energy to round-off. The ρ1/3

> q

prefactor makes the term vanish for uniform matter— so it is uniform-density-neutral and does not change the uniform-matter (Thomas–Fermi) limit—and removes the low-density singularity of a plain Weizs¨ acker-like form, so its Euler–Lagrange potential stays finite in the exponen-tial tail. It does not, however, repair the small uniform-limit error of the finite-table kernel itself. It penalizes spatial density gradients generally; for the tested A = 80 trajectories its largest numerical effect is in the central region. For the preliminary correction the released s2

scan ( results/preliminary correction/ ) shows that the gradient term does not remove the depletion: over the 8000-step budget the KS-initialized central density stays at ρc = 0 .061–0 .063 for s2 = 0–2 fm 4, and the de-pletion equally persists at the finer nμ = 48 quadrature. The instability is thus a property of the no-path fit itself, not of the discretization, and it is removed only at the fit stage by the energy-rise rows. The released shared func-tional needs no gradient regularization either, and adding one would not improve it: a targeted spot check at the production setting moves the KS-initialized A = 80 cen-tral density from ρc = 0 .1834 at s2 = 0 to 0 .1988 at 

s2 = 0 .5 fm 4, i.e. further from the KS value 0 .1769 rather than closer to it. The production shared functional there-fore uses s2 = 0; the gradient term of Eq. (29) remains implemented and released as optional machinery, with its exact discrete adjoint retained in the solver. Figure 4 and Table III show the released shared func-tional ( s2 = 0). All four final densities are free of the artificial tail peaks, central depletion, and oscillations de-scribed above. The transfer properties separate cleanly: 

• NLF shell structure transfers. The holdout dis-tance d = 0 .074 is comparable to the training val-ues (0 .096–0 .097)—indeed the smallest of the four; the interior oscillations of the unseen A = 140 sys-tem are reproduced across the interior region. At 

r = (1 .5, 3.0, 6.0) fm, respectively, the KS values are (0 .475 , 0.547 , 0.713) and the values at the self-consistent OFDFT density are (0 .476 , 0.530 , 0.591), consistent with structure-level transfer in this hold-out test. 

• Radii transfer : the universal-functional radii agree with KS to 0 .004–0 .033 fm across the four nuclei (0 .014 fm for the A = 140 holdout). Central den-sities are reproduced less accurately (4–12%), with 

> 16

O at the top of that range; for the released func-tional the A = 80 central density shifts by a further 7.8% between nμ = 24 and 48 (Appendix B). 

• Absolute energies do not transfer at this basis size: the holdout energy is overbound by 41 .7 MeV (2 .5%) in the nμ = 64 evaluation. During train-ing the per-nucleus energies are matched exactly on the fixed KS densities, but the A dependence of the energy integral is not captured by the shared 15 fit parameters (14 kernel-basis coefficients and the shift s). The working-discretization behavior above gives a nar-rower numerical lesson. First-order supervision on refer-ence densities does not by itself guarantee stable relax-ation after the functional and its quadrature have both been discretized. Stability tests must therefore vary not only the initial density and density-update algorithm but also the numerical representation of the nonlocal kernel. In the present case the gradient term is an effective regu-larizer at nμ = 24, whereas the disappearance of the de-pleted trajectory at nμ = 48 prevents us from identifying a corresponding instability of the quadrature-converged functional.  

> VI. DISCUSSION

What is learned. Figure 5 compares the learned kernel corrections. The per-nucleus δg are dominated by struc-ture at y ≲ 3, i.e., at inter-nucleon distances R ≲ 3/k F ,sharpening the short-range part of the kernel relative to the Lindhard-based g(y), with smaller adjustments in the first oscillation of the long-range response generated by the sharp Fermi surface. The shared correction has the 11                   

> FIG. 5. (a) Base dimensionless kernel g(y). (b) Learned cor-rections δg (y): per-nucleus fits, the shared multi-nucleus fit, and the Gaussian-process posterior mean ( ×10) with its fixed-active-set 2 σband.
> FIG. 6. Gaussian-process reformulation: posterior mean and pointwise fixed-active-set 2 σband of δg (y) (left); fixed-density NLF prediction for the previously unseen A= 140, obtained by propagating the posterior coefficient covariance to χat ρKS and then to the NLF (right). Here σis the local inverse-Hessian posterior standard deviation with the selected hyperparameters and final active penalty rows held fixed.

same qualitative pattern. The Gaussian-process poste-rior mean yields comparable fixed-density NLF accuracy with a much smaller peak amplitude, max |δg | = 0 .098 versus 4 .5 for the Gaussian-basis fit; its fitted local-correction shift is s = −8.32492. This peak-amplitude comparison is not a full effect-size statement—the two corrections carry different uniform moments and pair with different local-correction shifts—but the selected smooth prior shows that the large, largely canceling ba-sis components are not required to describe the training observations. 

Statistical interpretation and uncertainty. The Gaus-sian data groups are linear functionals of δg and there-fore define GP regression with operator observations (Ap-pendix C). The correlation length, prior amplitude, and noise scale are selected by marginal likelihood on the fi-nite grid of Appendix C; the reported point is not an unconstrained evidence optimum. The soft inequality penalties are included at the MAP level. The reported covariance is the inverse-Hessian Gaussian approxima-tion for the final quadratic subproblem, with the selected hyperparameters and active penalty rows held fixed; it is not a posterior conditioned on exact inequality con-straints. It propagates coefficient uncertainty through the linear response-ratio prediction at fixed ρKS ; it does not propagate uncertainty through the nonlinear ITE re-laxation and does not include model discrepancy. On the 80 radial points satisfying 1 < r < 8 fm and ρq >

10 −5 fm −3, the conditional 2 σ band covers 10 /80 = 0 .125 of the A = 140 response-ratio residuals. The remaining discrepancy is consistent with limited model expressivity, though likelihood/noise misspecification, hyperparame-ter uncertainty, and the fixed-density linear prediction are also possible contributors. 

Limitations and outlook. Four limitations are quan-tified above: the non-transfer of absolute energies (−41 .7 MeV at A = 140), the residual smooth NLF un-dershoot of the shared fit in the 16 O surface (0 .20 versus 0.41 at r = 4 .5 fm, a shared-capacity effect), and the growth of the energy drift with mass in per-nucleus fits, together with the residual percent-level quadrature sen-sitivity of the A = 80 central density. The first three motivate enrichment of the kernel-correction space (in-cluding possible t-dependence of δg beyond the shape correction), more training systems, and explicit uniform-matter constraints anchoring the A dependence of the energy. The fourth requires higher-order or more effi-cient angular integration before the A = 80 central den-sity or the nμ = 24 depletion artifact can be interpreted as a property of the continuum functional. The present gradient-dependent term should therefore be viewed as a limit-preserving numerical regularizer for the production discretization, not as a uniquely inferred physical cor-rection. Extension toward realistic functionals requires spin-orbit and Coulomb terms, isospin asymmetry, and deformation. The linear-in-kernel fitting strategy can be retained, but such extensions require suitable density-level representations of the additional terms. The bench-mark setting adopted here is that of Ref. [7], which allows direct comparison and isolates the shell-structure ques-tion.  

> VII. SUMMARY

We have demonstrated self-consistent orbital-free nu-clear DFT calculations with a learned nonlocal kinetic energy density functional. The key technical elements are (i) an analytic derivative of the represented finite-table density-dependent-kernel nonlocal term; (ii) functional-derivative-informed (Sobolev) fitting that exploits the linearity of the functional in the kernel shape; and (iii) ex-plicit stability checks across density-update algorithms, initial conditions, and numerical quadratures. A central-depletion instability exposed by a preliminary shared fit persists under gradient regularization and under a finer angular quadrature, and is removed only at the fit stage by the selected-path energy-rise penalty; a single limit-preserving gradient term is implemented and released but not required by the final functional. The per-nucleus fits impose exact KS-energy matching at the fixed KS train-ing densities; their final self-consistent energies are re-ported separately. They track the density and the full NLF shell structure of doubly-magic N = Z systems from A = 16 to A = 140, with the remaining central-12 density deviations quantified; a single universal correc-tion trained on A ≤ 80 transfers the shell structure and radius to a previously unseen A = 140 test system, while absolute energies remain nucleus-specific at the present basis size. For the base constant-kF 16 O bench-mark, the rearranged diagonalization converges without mixing in 41 density-update iterations at λ = 3 and agrees with a prolonged ITE trajectory to 0 .368 keV and a relative density difference of 4 .59 × 10 −4. The learned-functional observables reported here are evalu-ated on the final ITE densities, which are cross-checked by the rearranged diagonalization. A Gaussian-process reformulation supplies finite-grid marginal-likelihood se-lection and conditional parameter-uncertainty quantifi-cation. These results turn the nonlocal-functional pro-gram of Ref. [7] into a self-consistent scheme and iden-tify the concrete ingredients—analytic derivatives, solver cross-checks, quadrature control, and energy anchoring— needed for a universal orbital-free nuclear functional. 

> DATA AVAILABILITY

The solver, all calculation inputs, the analysis scripts, and the data underlying the tables and fig-ures are available at https://github.com/fimoto/ nofdft-nuclear-kedf .

> ACKNOWLEDGMENTS

The author received no funding or other support and acknowledges the use of AI-based assistants—Claude (Claude Opus 4.8, Anthropic) and GPT-5.6 (OpenAI)— as interactive aids to develop and debug some part of the author’s numerical codes during this work. 

> Appendix A: Imaginary-time-evolution details

ITE scheme. Define the real density amplitude 

ϕq (r) = pρq (r), with units fm −3/2 and R d3r |ϕq |2 = Nq .Suppressing q, one symmetric second-order split step for the Hamiltonian frozen at iteration k is 

ϕ(k+1) = PNq e−∆tv loc [ρ(k)]/2e−∆t ˆTvW e−∆tv loc [ρ(k)]/2ϕ(k).

(A1) Here ∆ t is an imaginary-time step with units MeV −1,

PNq rescales the updated amplitude to particle num-ber Nq , and ˆTvW = −C∇2 is the von Weizs¨ acker ki-netic operator. The multiplicative remainder is vloc =

vkernel +vcorr +v∇ +vSkP , where vkernel ≡ vNL includes the learned kernel correction, vcorr is the response of the lo-cal Thomas–Fermi correction, vSkP = δE SkP int /δρ tot , and 

v∇ = 0 for s2 = 0. The von Weizs¨ acker propagator is applied to u = rϕ with the Dirichlet second-difference operator at the nr = 250 interior points ri = i∆r, where ∆r = Rbox /(nr + 1) and Rbox = 20–22 fm. The lo-cal exponential argument is clamped to [ −80 , 80] to pre-vent overflow. The fixed-step split update itself has no energy-based rejection; only the adaptive controller (be-low) rejects and rolls back. Energies are monitored at the first step and then every 20 iterations for the per-nucleus runs and every 50 for the shared multi-nucleus runs. We distinguish the number of attempted steps (the iteration budget, here 8000) from the number of accepted moni-tor points (the scheduled monitor points that pass the energy-acceptance test); with the adaptive controller the monitored energy is non-increasing across the accepted points by construction, whereas for a plain fixed-step run monotonic decrease is only an observed property. The fi-nal summary is recomputed on the written final density. The explicit-potential step has a stiffness-dependent step-size ceiling (∆ t ≲ 5 × 10 −5 MeV −1; the von Weizs¨ acker propagator itself is unconditionally sta-ble, being an exact exponential of the frozen second-difference operator, so the ceiling comes from the explicit local potential and the splitting error, not from the ki-netic term). A small fixed step then relaxes the light nuclei fully but leaves the heavy systems short of station-arity: at ∆ t = 10 −5 over 8000 steps the final EL residu-als ∥ˆhϕ − μϕ ∥L2 are 0 .03/0 .09 ( 16 O/ 40 Ca) but 0 .74/0 .52 (A=80/ A=140) per-nucleus, the heavy densities being geometrically settled ( ρc, rrms stable to < 1%) but the functional not yet stationary. The reported species resid-ual is REL ,q of Eq. (19); its normalization and units therefore remain the same in the ITE and diagonaliza-tion checks. We adapt the step size as follows. At an accepted monitor point, ∆ t is multiplied by 1 .1, up to 5 × 10 −5 MeV −1. If the monitored energy exceeds the previous accepted value by more than 10 −12 MeV, the trial density is rejected, the previous accepted density is restored, and ∆ t is multiplied by 0 .5, with a lower bound of 10 −8 MeV −1. This drives the heavy per-nucleus resid-uals to 0 .02–0 .08 at the same 8000-step budget without changing the converged geometry, and yields the con-verged densities quoted in Tables II–III. As an indepen-dent check, the rearranged diagonalization reaches the same fixed points (Sec. II C). The symmetric split form is held fixed throughout the comparisons. 

Initialization. Production runs are initialized at the KS density (Sec. II C). As a global-convergence probe we also evolved the 16 O per-nucleus functional from the smooth, featureless profile 

ρq (r) = ρ0q

1 + exp[( r − r0)/a F ] ,

4π

Z Rbox 

> 0

r2ρq (r) dr = Nq , (A2) with r0 = 3 .0 fm; the normalization determines ρ0q and 

aF = 0 .55 fm is the surface diffuseness. The monotone flow closes ∼ 98% of the energy gap over 4 .0 × 10 4 iter-ations ( E = −124 .6 versus −126 .9 MeV; final EL resid-ual 4 .2), with the density approaching the KS-initialized final density (weighted L2 difference 8%; rrms = 2 .89 13 versus 2 .74 fm), with no tendency toward a distinct self-consistent solution along the observed trajectory. Fur-ther relaxation from featureless profiles is limited by slow imaginary-time dynamics far from the KS-initialized final density at the fixed small step. This is a solver-efficiency issue, not evidence for a separate minimum of the learned functional. 

> Appendix B: Numerical verification and settings

Analytic EL potentials are verified against second-order central finite differences of the total energy (rela-tive agreement down to ∼ 10 −11 at the optimal finite-difference perturbation amplitude), separately for the base kernel and for each δg basis function. The gradient regularization term of Eq. (29) is implemented so that its analytic potential is the exact discrete adjoint of the first-difference energy. For one species, 

Edisc  

> ∇,q

= s2C X

> i

Wiρ1/3 

> qi

(Dρq )2 

> i

, Edisc  

> ∇

= X

> q=n,p

Edisc  

> ∇,q

,

(B1) where ρq = (ρq1, . . . , ρ qn r )T, Wi = 4πr 2 

> i

wi con-tains the implemented radial quadrature weight wi, and 

D uses centered differences in the interior and one-sided differences at the two endpoints. With W =diag( W1, . . . , W nr ) and ⊙ denoting elementwise multipli-cation, the potential placed on the same grid is 

vdisc  

> ∇,q

= s2C

 13 ρ−2/3 

> q

⊙ (Dρq )2

+2 W −1DTW {ρ1/3 

> q

⊙ Dρq }

i

. (B2) Thus vdisc  

> ∇,q,i

= W −1 

> i

∂E disc  

> ∇,q

/∂ρ qi . Density powers are eval-uated with the code floor max( ρqi , 10 −20 fm −3) to avoid numerical singularities. The resulting directional deriva-tive matches finite differences to round-off ( ∼ 3 × 10 −11 

relative), like the von Weizs¨ acker and Skyrme terms, and the potential remains bounded in the exponential tail (|v∇| ≲ 10 −3 for r > 8 fm) because of the ρ1/3 pref-actor. The von Weizs¨ acker and Skyrme-gradient terms use the same centered-interior/one-sided-endpoint first derivative (with exact discrete adjoints in the correspond-ing gradients), while the imaginary-time kinetic propa-gator uses the Dirichlet second-difference operator. On the smooth final production densities, the two integrated von Weizs¨ acker energies differ by 0 .023–0 .105 MeV per species (0 .04–0 .06%). The ITE is therefore not the ex-act discrete gradient flow of the first-difference energy, although the integrated discrepancy is small on the re-ported smooth densities. The linearity of the functional in g is verified by direct response-assembly tests, and an independent high-order angular quadrature with nμ = 96 checks the angular convolution separately from the mon-itored production discretization. Three angular settings are used for different stages of the calculation; Table IV states both the density on which each is evaluated and the quantity reported. Writing the Fourier momentum as q = kF ¯q, the di-mensionless transform is truncated at ¯ qmax = 80 (code keyword xqmax ) and evaluated by composite Simpson quadrature with N¯q = 6000 even subintervals (nq ). The kernel is stored at Ny uniformly spaced nodes, including both endpoints, on 0 ≤ y ≤ ymax , and g and 

g′ are evaluated from the same natural cubic spline; both are set to zero beyond ymax . The adaptive tables use ( ymax , N y ) = (102 .718 , 12840) for the light systems and (112 .990 , 14124) for the heavier systems. The 

nμ = 48 and 64 calculations are diagnostic reevaluations rather than the angular quadrature that generated the ITE trajectories. The NLF is defined from the kinetic kernel using the convention of Eq. (3); the local gradient regularization term of Eq. (29) enters the energy and the EL potential but is not folded into this kinetic-energy-density convention, so the reported NLF curves are a kernel diagnostic and are unchanged by s2 at fixed density. The angular-quadrature error is not negligible at the derivative level: core relative errors are at the few-percent level, and the offset between the monitored nμ = 24 energy and the nμ = 64 readout reaches 14 .8 MeV for the A = 80 per-nucleus functional (2 .1 MeV for the shared functional). Atargeted 8000-step ITE recomputation of the released 

shared functional at nμ = 48 (released with the data, 

results/preliminary correction/nmu48 released/ )shifts the A = 80 endpoint moderately: the central density moves from 0 .1834 to 0 .1977 fm −3 (7 .8%), the rms radius by 0 .004 fm, the NLF distance from 0 .096 to 0.094, and the nμ = 64 energy readout of the endpoint by 0 .12 MeV. For the preliminary no-path correction the central depletion persists unchanged at nμ = 48. Thus the radius and integrated NLF diagnostic are comparatively stable, while the A = 80 central density retains a percent-level angular-quadrature sensitivity. Using nμ = 64 for all tabulated energies provides a common comparison convention, not a convergence certificate. Fit hyperparameters: the EL-potential-data weight is 

β = 2 ( βA=80 = 12 in the joint fit); the surface–tail-data weight is ηT = 0 .3 (1 .2 for 16 O in the final shared multi-nucleus fit); the tail-margin and selected-path energy-rise weights are 12 and 10, with target margins χmin = 5 and 

ε = 2 MeV, respectively. The sets of violated rows and their fixed-within-solve normalizations are updated be-            

> TABLE IV. Roles of the angular quadratures. “Final ITE(24) density” means the density produced by the nμ= 24 trajec-tory; reevaluation does not restart the SCF evolution. Purpose density nμreported quantity ITE trajectory evolving 24 density, ∆ E,REL
> Energy reevaluation final ITE(24) 64 tabulated energy NLF reevaluation final ITE(24) 48 NLF curve and dNLF

14 tween least-squares solves until they no longer change. This is the active-set repeated penalized least-squares it-eration of Eqs. (25) and (26). The released GP imple-mentation performs 16 updates; its active rows and co-efficients cease to change at update 14 for the released data. All reported GP quantities are generated from the final update. The value, EL-potential, and tail data groups are normalized per nucleus by their weighted sig-nal power. 

> Appendix C: Gaussian-process formulation

The kernel correction is expanded in a reduced-rank Gaussian-process basis [20], δg (y) = PMj=1 wj φj (y), where 

φj (y) = 

r 2

L sin 

 πjy L



, 0 ≤ y ≤ L. (C1) The code sets φj (y) = 0 for y > L . The retained rank is 

M = 20 and the dimensionless interval length is L = 12. The mode index is j = 1 , . . . , M , wj is its dimensionless random coefficient, and the basis functions are orthonor-mal on [0 , L ]. Defining Ω j = πj/L , the prior is 

wj ∼ N [0 , S SE (Ω j )] ,SSE (Ω) = σ2

> f

√2π ℓ GP exp( −ℓ2GP Ω2/2) ,SGP = diag {SSE (Ω 1), . . . , S SE (Ω M )}. (C2) Here ℓGP is the correlation length in the dimensionless 

y coordinate and σf is the prior amplitude. The dis-tinct symbol ℓGP avoids confusion with orbital angular momentum. The full vector solved by the GP implementation is 

ϑ = ( w1, . . . , w M , s, μ 1, . . . , μ Ntr )T, Ntr = 3 , (C3) where s is the local-correction shift and the last three en-tries are the auxiliary chemical potentials of the training nuclei. The prior precision S−1GP acts on the wj block. Weak fixed diagonal regularizers 10 −4 and 10 −10 are ap-plied to s and the μν entries, respectively, only to regu-larize otherwise uninformative directions. All Gaussian data groups of Sec. III are linear in ϑ, and the MAP coefficients are obtained with the same bordered KKT construction used for the exact energy equalities. Hyperparameters (ℓGP , σ f ) and a global likelihood scale κ are compared by log marginal likelihood over the finite grid ℓGP ∈ { 0.6, 1, 1.5, 2.2}, σf ∈ { 0.3, 1, 3}, and 

κ ∈ { 30 , 100 , 300 , 1000 }. The scalar κ multiplies the com-mon precision of the normalized Gaussian observation groups; it changes their likelihood weight relative to the GP prior without changing their internal relative weights. The finite-grid maximum is ( ℓGP , σ f , κ ) = (1 .5, 0.3, 300); it is not an unconstrained optimum, and σf lies at the grid edge. Fixing the noise scale to the signal power drives the finite-grid evidence toward δg → 0, motivating the separate likelihood scale. The evidence scores only the Gaussian value, EL-potential, and surface–tail data groups. The exact energy equalities and soft quadratic penalties enter only at the MAP stage. The released implementation performs 16 active-set updates; the ac-tive rows and coefficients cease to change at update 14 for the released data. The resulting correction has max |δg | = 0 .098 and local-correction shift s = −8.32492. For completeness, the released GP implementation specializes the weights in Eq. (25) as follows. If χ(0)  

> νi

de-notes the response ratio before the fitted GP correction, define 

eZ(k) 

> T,ν

= X

> i∈A (k)
> T ,ν

[χmin − χ(0)  

> νi

]2. (C4) The two normalization scales are 

Z(k) 

> T,ν

= max { eZ(k) 

> T,ν

, 10 −12 },ZS = (10 MeV) 2. (C5) For compactness, define the active squared residuals 

R(k) 

> T,ν

(ϑ) = X

> i∈A (k)
> T ,ν

[χνi (ϑ) − χmin ]2,

R(k) 

> S

(ϑ) = X

> (ν,j )∈A (k)
> S

[∆ Eνj (ϑ) − ε]2,

R(k) 

> T

(ϑ) = X

> ν

R(k) 

> T,ν

(ϑ)

Z(k)

> T,ν

. (C6) The quadratic contribution held fixed during update k+1 is then 

P(k)ineq (ϑ) = κη H

2 R(k) 

> T

(ϑ)+ κη S

2ZS

R(k) 

> S

(ϑ). (C7) Here ηH = 12 and ηS = 10 are the released tail and selected-path penalty weights. Equations (C5) and (C7) reproduce the normal-matrix additions in the released 

gp fit.py . The selected tail grid points enter with equal weight; there is no additional radial quadrature weight in this penalty. The 10 −12 floor only avoids division by zero. 

Z(k) 

> T,ν

is recomputed when the tail active set changes and then held fixed during the following equality-constrained least-squares solve. By contrast, ZS is constant. All three selected-path penalty rows come from the training A = 80 system; no A = 140 path is used. At the released final MAP solution the three rows remain active, with ∆ E = 0 .6351, 0 .4898, and 1 .6561 MeV. Thus ε =2 MeV is a soft target margin rather than a guaranteed lower bound. This numerical fact is also why these terms must not be described as hard stability constraints. With the selected hyperparameters and final active penalty rows held fixed, let H be the regularized nor-mal matrix of the final quadratic subproblem in the full 15 

ϑ space, including the active penalty rows, and let G con-tain the exact energy-equality rows. The inverse-Hessian Gaussian approximation, projected onto the equality subspace, is Σ = H−1 − H−1GT(GH −1GT)−1GH −1, (C8) where the superscript T denotes transpose. This subtrac-tion projects H−1 onto coefficient variations that satisfy the exact linear equalities. It is a local Gaussian approx-imation to the final fixed-active-set quadratic subprob-lem, not a posterior truncated or conditioned by exact inequalities. For an affine prediction χ(r) = χ(0) (r) + a(r)Tϑ, the propagated variance is 

σ2

> χ

(r) = a(r)TΣa(r),σNLF (r) ≃ −2χ(r)[1 + χ2(r)] 2 σχ(r), (C9) where the second expression is first-order propagation through NLF = 1 /(1 + χ2). The same Σ is propagated to δg (y) and to the fixed-density A = 140 response-ratio prediction shown in Fig. 6. It does not include hyperparameter uncertainty, uncertainty about which inequalities are active, nonlinear propagation through ITE, or model discrepancy. The conditional 2 σ cov-erage is 10 /80 = 0 .125 on the radial mask stated in Sec. VI. The full finite-grid evidence selection, converged MAP fit, conditional covariance, and coverage diag-nostics are reproduced by a single self-contained script (gp fit.py ) with its staged training inputs in the accom-panying archive; it regenerates the selected hyperparam-eters ( ℓGP , σ f , κ ) = (1 .5, 0.3, 300), the local-correction shift s = −8.32492, the posterior mean and 2 σ band, and the A = 140 holdout coverage. 

[1] P. Hohenberg and W. Kohn, Phys. Rev. 136 , B864 (1964). [2] W. Kohn and L. J. Sham, Phys. Rev. 140 , A1133 (1965). [3] M. Bender, P.-H. Heenen, and P.-G. Reinhard, Rev. Mod. Phys. 75 , 121 (2003). [4] Y. A. Wang and E. A. Carter, in Theoretical Methods in Condensed Phase Chemistry (Kluwer, Dordrecht, 2000), pp. 117–184. [5] Q. Xu, C. Ma, W. Mi, Y. Wang, and Y. Ma, WIREs Comput. Mol. Sci. 14 , e1724 (2024). [6] X. H. Wu, Z. X. Ren, and P. W. Zhao, Commun.Phys. 

8, 316 (2025). [7] X. H. Wu, G. Col` o, K. Hagino, and P. W. Zhao, Phys. Rev. Lett. 136 , 092501 (2026). [8] L.-W. Wang and M. P. Teter, Phys. Rev. B 45 , 13196 (1992). [9] Y. A. Wang, N. Govind, and E. A. Carter, Phys. Rev. B 

60 , 16350 (1999). [10] C. Huang and E. A. Carter, Phys. Rev. B 81 , 045206 (2010). [11] L. A. Constantin, E. Fabiano, and F. Della Sala, Phys. Rev. B 97 , 205137 (2018). [12] W. Mi, A. Genova, and M. Pavanello, J. Chem. Phys. 

148 , 184107 (2018). [13] J. C. Snyder, M. Rupp, K. Hansen, K.-R. M¨ uller, and K. Burke, Phys. Rev. Lett. 108 , 253002 (2012). [14] L. Li, J. C. Snyder, I. M. Pelaschier, J. Huang, U.-N. Niranjan, P. Duncan, M. Rupp, K.-R. M¨ uller, and K. Burke, Int. J. Quantum Chem. 116 , 819 (2016). [15] R. Meyer, M. Weichselbaum, and A. W. Hauser, J. Chem. Theory Comput. 16 , 5685 (2020). [16] F. Brockherde, L. Vogt, L. Li, M. E. Tuckerman, K. Burke, and K.-R. M¨ uller, Nat. Commun. 8, 872 (2017). [17] X. H. Wu, Z. X. Ren, and P. W. Zhao, Phys. Rev. C 105 ,L031303 (2022), arXiv:2105.07696. [18] J. Dobaczewski, H. Flocard, and J. Treiner, Nucl. Phys. A 422 , 103 (1984). [19] F. Imoto, M. Imada, and A. Oshiyama, Phys. Rev. Re-search 3, 033198 (2021). [20] A. Solin and S. S¨ arkk¨ a, Stat. Comput. 30 , 419 (2020).