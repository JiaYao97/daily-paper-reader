---
title: Physics-guided spatiotemporal neural models for fuel density prediction
title_zh: 物理引导的时空神经模型用于燃料密度预测
authors: "Tolga Caglar, Jaynil Jaiswal, Saqib Azim, Yudhir Gala, Mai H. Nguyen, Ilkay Altintas"
date: 2026-07-08
pdf: "https://arxiv.org/pdf/2607.06999v1"
tags: ["query:flow-pred"]
score: 8.0
evidence: 使用神经算子和物理约束的时空燃料密度预测
tldr: 燃料密度预测对火灾管理至关重要。本文提出物理引导的机器学习框架，集成ConvLSTM、AFNONet和ViViT三种深度学习架构，在损失函数中加入质量守恒和扩散速率可微物理项。实验结果平均显示，该方法在准确性和稳定性上均优于纯数据驱动基线。该框架实现了高效且物理合理的火灾预测，为自适应烧除管理提供支持。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06999v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 913, \"height\": 505}, {\"url\": \"assets/figures/arxiv/2607.06999v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1871, \"height\": 472}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06999v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1553, \"height\": 332}]"
motivation: 提升燃料密度预测的准确性与稳定性，通过融入物理知识克服纯数据驱动方法的局限。
method: 提出PGML框架，使用ConvLSTM、AFNONet、ViViT三种架构，在损失函数中加入可微物理约束项。
result: 在多个独立试验中，PGML框架的平均预测准确性和稳定性均优于无物理约束的基线模型。
conclusion: 实现了高效、物理合理的火灾预测，可支持自适应烧除管理决策。
---

## 摘要
本文提出了一种物理引导的机器学习（PGML）框架用于燃料密度预测，将物理约束和领域知识集成到深度学习模型中，以提高模型的准确性和稳定性。我们探索了三种深度学习架构——ConvLSTM、自适应傅里叶神经算子（AFNONet）和视频视觉Transformer（ViViT）——来建模燃料密度的时空演化。我们的方法在损失函数中加入了可微分的物理信息项，包括质量守恒的燃料传输项和蔓延速率估计。多次独立试验的平均结果表明，所提出的PGML框架在准确性和稳定性上均优于没有物理约束的纯数据驱动基线。该框架实现了计算高效、物理合理的火灾预测，以支持自适应规定燃烧管理。

## Abstract
This paper presents a physics-guided machine learning (PGML) framework for fuel density prediction, integrating physics constraints and domain knowledge into deep learning models to enhance model accuracy and stability. We explore three deep learning architectures -- ConvLSTM, Adaptive Fourier Neural Operator (AFNONet), and Video Vision Transformer (ViViT) -- to model the spatiotemporal evolution of fuel density. Our approach incorporates differentiable physics-informed terms in the loss function, including a mass-conserving fuel transport term and a rate-of-spread estimation. Experimental results, averaged across multiple independent trials, demonstrate that the proposed PGML framework outperforms purely data-driven baselines without physics constraints in both accuracy and stability. This framework enables computationally efficient, physically plausible fire forecasting to support adaptive prescribed burn management.

---

## 论文详细总结（自动生成）

# 物理引导的时空神经模型用于燃料密度预测——中文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：野火每年在美国烧毁数百万英亩土地。为降低风险，消防管理者采用规定燃烧（prescribed burns），但需要精确预测火势行为。传统过程模型（如FARSITE、QUIC-Fire）物理精确但计算成本高，无法支持实时决策。纯数据驱动深度学习模型（如CNN、U-Net）计算快，但常预测非物理行为（如燃料自发再生），且难以处理多点点火。
- **核心问题**：如何在不牺牲计算效率的前提下，使深度学习模型预测出物理一致的燃料密度演化？
- **整体含义**：提出一种物理引导的机器学习（PGML）框架，将领域知识以“软惩罚”形式嵌入损失函数，而非刚性的PDE求解，从而在保持计算快速的同时提高预测的物理合理性与稳定性，为自适应规定燃烧管理提供支撑。

## 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：构建一个通用框架，将可微分的物理约束项加入损失函数，可应用于多种时空深度学习架构。物理约束包括质量守恒（禁止燃料再生）、燃烧/未燃烧区域的状态加权、以及火线蔓延速率（ROS）的拟合。

**关键技术细节**：

- **模型架构**：采用三种代表性架构：
  - **ConvLSTM**：两层的卷积LSTM（64个3×3滤波器），加入残差连接和3D时空平滑（3×3×3核），使用LeakyReLU防止“死神经元”。
  - **Video Vision Transformer (ViViT)**：将输入视频序列通过tubelet嵌入分词，经自注意力处理后再通过MLP和Conv3D解码回燃料密度图。
  - **Adaptive Fourier Neural Operator (AFNONet)**：用3D patch嵌入形成时空token，通过傅里叶域混合（复杂度O(N log N)）替代自注意力，解码时通过pixel-shuffle重建输出序列。
- **物理引导损失函数（WiFireLoss）**：
  1. **基础MSE（L<sub>MSE</sub>）**：像素级均方误差。
  2. **燃料传输损失（L<sub>fuel</sub>）**：惩罚预测中燃料密度增加（即非物理再生），取所有时间步上预测值大于上一时间步的像素与真值的MSE。
  3. **状态加权损失（L<sub>burn</sub>, L<sub>unburn</sub>）**：利用可微的sigmoid掩模（温度尺度T=0.02）区分燃烧区（燃料密度≤0.665）和未燃烧区，分别加权MSE。
  4. **蔓延速率损失（L<sub>ROS</sub>）**：利用可微近似计算火线前缘位置（通过softmax加权平均代替argmax），进而计算ROS，约束预测ROS与真值ROS的误差。
- **总损失**：L<sub>total</sub> = λ<sub>MSE</sub>L<sub>MSE</sub> + λ<sub>fuel</sub>L<sub>fuel</sub> + λ<sub>burn</sub>L<sub>burn</sub> + λ<sub>unburn</sub>L<sub>unburn</sub> + λ<sub>ROS</sub>L<sub>ROS</sub>。权重通过经验调优：λ<sub>MSE</sub>=1, λ<sub>fuel</sub>=0.01, λ<sub>ROS</sub>=0.01, λ<sub>burn</sub>=0.1, λ<sub>unburn</sub>=0.1。

## 3. 实验设计

- **数据集**：使用QUIC-Fire仿真生成的指定燃烧场景。模拟时长50秒，空间网格300×300（平坦草地）。包含7种风速（1-15 m/s）、11种风向（230°-330°）、4种点火模式（空中、向内、向外、条带状），共308种组合。燃料密度范围[0, 0.7 kg/m²]。
- **输入输出结构**：输入6帧序列（T<sub>in</sub>=6），预测接下来3帧（T<sub>out</sub>=3）。每个样本含4通道：源图、风速、风向、点火模式。输出为1通道燃料密度图。图像裁剪至296×296。时间步长为1，滑动窗口。
- **基准与对比**：对比三种模型（ConvLSTM、AFNONet、ViViT）在不加物理损失（仅MSE）和加入WiFireLoss两种情况。每种配置独立运行3次取均值和标准差。
- **评估指标**：Base MSE、Burn MSE、Unburn MSE、Fuel Transport MSE、ROS MSE、Total Loss（即L<sub>total</sub>）。

## 4. 资源与算力

- **硬件**：单张NVIDIA RTX 3090 Ti GPU。
- **精度**：bfloat16混合精度训练。
- **超参数**：
  - ConvLSTM：15 epochs，batch size 4，早停patience 5。
  - AFNONet：50 epochs，batch size 8，早停patience 10。
  - ViViT：未明确epoch数，但同样使用早停。
- **训练时间**：论文未具体给出各模型训练时长，但基于上述配置和单卡RTX 3090 Ti，推测训练时间在数小时以内。

## 5. 实验数量与充分性

- **实验数量**：共6种模型配置（3模型 × 2 loss变体），每种运行3次，总计18次实验。提供了表I展示均值和标准差。
- **充分性**：
  - **优点**：多次独立运行提供统计意义，环境变量（风速、风向、点火模式）覆盖较广（308组合）。
  - **不足**：未进行消融实验分析各物理损失项单独贡献；未对比其他物理引导方法（如PINN或硬约束）；仅在单一地形（平坦草地）和单一燃料类型上测试；预测窗口仅3帧（6秒），未评估长期预测。整体实验设计相对有限，但足以证明方法有效性。

## 6. 论文的主要结论与发现

- 集成物理引导损失（WiFireLoss）在所有三种架构上均降低了误差均值和标准差，表明物理约束同时提升了预测准确性和稳定性。
- PG-AFNONet表现最优（总损失0.017，显著低于纯AFNONet的0.028）。
- 定性结果（图2）显示预测燃料密度图与真实图视觉高度一致。
- 框架具有良好的通用性，可轻松扩展到其他时空预测模型。

## 7. 优点

- **方法通用性**：提出的物理损失函数可即插即用于CNN、Transformer、神经算子等多种架构，不依赖于特定模型。
- **软约束避免数值问题**：使用可微的“软惩罚”而非刚性PDE约束，避免了传统PINN的梯度流病态问题。
- **物理相关性强**：约束直接对应火灾管理中的关键物理量（质量守恒、燃烧/未燃烧状态、火蔓延速率），生成的结果更具实际意义。
- **实验设计规范**：多次独立试验报告均值和标准差，结果可靠。

## 8. 不足与局限

- **数据局限性**：仅使用仿真数据，且限于平坦草地、单一燃料类型、恒定风速风向。真实火灾场景（复杂地形、多种植被、动态风场）未验证，存在域迁移风险。
- **预测窗口短**：仅预测未来3帧（对应3秒？原文未明确帧间隔，但模拟总时长50秒/300×300网格，可能每帧约0.167秒？）实际应用需要更长时间预报。
- **缺乏消融研究**：未对各物理损失项（如仅保留L<sub>fuel</sub>或L<sub>ROS</sub>）进行单独分析，无法确定每项的具体贡献。
- **对比不全面**：未与硬约束方法（如投影法）或更先进的物理信息网络（如PINN）比较，也未与准稳态物理模型混合方法对比。
- **计算资源单一**：仅在单卡上进行实验，未讨论多GPU或分布式训练扩展性。
- **未公开代码与数据**：论文未提及开源，可复现性受限。

（完）
