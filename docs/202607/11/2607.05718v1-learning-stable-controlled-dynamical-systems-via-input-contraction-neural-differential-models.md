---
title: Learning Stable Controlled Dynamical Systems via Input-Contraction Neural Differential Models
title_zh: 通过输入收缩神经微分模型学习稳定的受控动力系统
authors: Syed Pouladi
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.05718v1"
tags: ["query:pc-latent"]
score: 8.0
evidence: 学习带外部输入的稳定潜在动力学；匹配潜在动力学和物理约束学习
tldr: 现有神经微分方程在处理控制输入时缺乏结构保证或仅适用于恒定输入。本文提出输入收缩神经微分模型，通过输入依赖收缩正则化同时学习非自主向量场和黎曼度量，确保时变输入下增量指数收敛。在混沌振荡器和永磁同步电机系统上，该模型大幅降低长时域预测误差并对输入扰动保持鲁棒。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05718v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 818, \"height\": 473, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05718v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 835, \"height\": 466, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05718v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1071, \"height\": 312, \"label\": \"Table\"}]"
motivation: 现有神经微分方程无法同时保证时变控制输入下的稳定性和结构鲁棒性。
method: ICNDM嵌入输入编码器和参数化度量网络，通过输入依赖收缩正则化联合学习非自主向量场和广义收缩度量。
result: 在混沌振荡器和PMSM系统上，长时域误差显著降低，且对输入扰动具有强鲁棒性。
conclusion: ICNDM为数据驱动控制提供了兼具稳定性和泛化性的连续时间模型框架。
---

## 摘要
从观测数据中学习动力系统的连续时间表示已成为数据驱动控制和科学机器学习的基石。然而，现有的神经微分方程要么启发式地处理外部控制输入而不提供严格的结构保证，要么在恒定或消失输入的严格假设下强制实现稳定性。本文提出了输入收缩神经微分模型（ICNDM），这是一种新颖的深度学习框架，能够无缝地整合时变控制输入，同时通过依赖于输入的收缩正则化确保增量指数收敛。通过利用嵌入式输入编码器和参数化度量网络，所提出的架构同时学习了非自主神经向量场和广义黎曼收缩度量。我们推导了依赖于输入的收缩的充分条件，并正式建立了在有界外部激励下的输入到状态收缩性质。在高度非线性混沌振荡器和永磁同步电机（PMSM）驱动系统的实验数据上进行的大量数值评估表明，与最先进的神经微分基准相比，ICNDM在长时间滚动误差方面大幅降低，并且在输入扰动下展现出卓越的结构鲁棒性。

## Abstract
Learning continuous-time representations of dynamical systems from observation data has emerged as a cornerstone of data-driven control and scientific machine learning. However, existing neural differential equations either treat external control inputs heuristically without providing strict structural guarantees, or enforce stability properties under the restrictive assumption of constant or vanishing inputs. This paper proposes the Input-Contraction Neural Differential Model (ICNDM), a novel deep learning framework that seamlessly incorporates time-varying control inputs while ensuring incremental exponential convergence via input-dependent contraction regularization. By leveraging an embedded input encoder and a parameterized metric network, the proposed architecture learns both the non-autonomous neural vector fields and a generalized Riemannian contraction metric simultaneously. We derive sufficient conditions for input-dependent contraction and formally establish an input-to-state contraction property under bounded external excitations. Extensive numerical evaluations on highly nonlinear chaotic oscillators and experimental data from a Permanent Magnet Synchronous Motor (PMSM) drive system demonstrate that ICNDM yields substantial reductions in long-horizon rollout errors and exhibits superior structural robustness against input perturbations compared with state-of-the-art neural differential benchmarks.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Learning Stable Controlled Dynamical Systems via Input-Contraction Neural Differential Models》的详细中文总结。

---

## 1. 论文的核心问题与整体含义

**研究动机与背景**：
- 从观测数据中学习动力系统的连续时间表示是数据驱动控制与科学机器学习的核心任务。
- 现有神经微分方程（如 Neural ODE、Neural CDE）在处理外部控制输入时，要么缺乏严格的稳定性和结构保证，要么仅在恒定或消失输入的假设下才能保持收缩性。
- 实际工程系统（如机器人、电机驱动）常受到时变、非自主控制输入的影响，因此亟需一种既能建模非自主动态，又能保证长时域稳定和输入鲁棒性的方法。

**核心问题**：
- 如何在神经微分方程框架中同时实现（1）对任意时变控制输入的表征，（2）增量指数收敛的严格保证，以及（3）对外部扰动或输入噪声的鲁棒性。

**整体含义**：
- 提出 Input-Contraction Neural Differential Model (ICNDM)，首次将输入依赖的收缩分析与神经微分方程结合，为受控动力系统的数据驱动辨识提供了兼具稳定性和泛化性的解决方案。

---

## 2. 方法论：核心思想、关键技术细节

**核心思想**：
- 将时变控制输入显式编码为高维特征，并通过联合学习向量场和黎曼收缩度量，使得系统在任意输入下满足输入依赖的收缩条件，从而保证状态轨迹间的指数收敛。

**关键技术细节**：

1. **模型架构**：
   - 状态方程：\(\dot{x} = f_\theta(x, E_\phi(u))\)，其中 \(E_\phi\) 是输入编码器，将原始控制信号 \(u\) 映射到潜在空间。
   - 同时训练一个深度度量网络 \(M_\omega(x)\) 用于定义黎曼度量。

2. **度量正则化**：
   - 度量矩阵通过 Cholesky 分解保证正定性：\(M_\omega(x) = L_\omega(x)L_\omega(x)^T + \epsilon I\)。
   - 定义收缩矩阵 \(\Psi(x,u) = A^T M + M A + \dot{M}\)，其中 \(A = \partial f_\theta / \partial x\)，\(\dot{M}\) 是度量沿轨迹的方向导数。

3. **损失函数**：
   - 数据拟合损失 \(L_{\text{pred}}\)：多步预测的均方误差。
   - 收缩约束损失 \(L_c\)：惩罚 \(\Psi + \beta M\) 的最大特征值大于零的情况，强制满足 \(\Psi \preceq -\beta M\)。
   - 输入灵敏度正则化 \(L_u\)：对输入 Jacobian 施加 Frobenius 范数惩罚，降低对高频噪声的敏感度。
   - 总损失：\(L = L_{\text{pred}} + \lambda_c L_c + \lambda_u L_u\)。

4. **理论保证**：
   - **定理1**：若 \(L_c=0\) 即收缩条件成立，则任意两个相同输入下的轨迹指数收敛，误差以 \(\sqrt{\alpha_2/\alpha_1} e^{-\beta t/2}\) 衰减。
   - **定理2**：若向量场对输入 Lipschitz 连续，则不同输入下的轨迹误差满足输入到状态收缩（ISC）界，即误差受初始误差和输入差的加权和约束。

---

## 3. 实验设计

**数据集/场景**：
- **非线性混沌振荡器**：Duffing 振荡器、Van der Pol 振荡器（多频正弦输入驱动）。
- **物理系统**：永磁同步电机（PMSM）驱动系统（状态：\(i_d, i_q\)；输入：\(v_d, v_q, \omega_e\)；具有强交叉耦合和非线性）。

**基准方法**：
1. Standard Neural ODE [6]（输入简单拼接）
2. Controlled Neural ODE [7]（基于路径的控制输入）
3. ICODE [1]（专门建模外部输入）
4. ControlSynth Neural ODE [2]（强调收敛保证）
5. Differential Neural Network (DNN) [3]（工业结构稳定网络）

**对比指标**：
- 1步预测 MSE 和 200步（或500步）长时域滚动 MSE。
- 输入噪声鲁棒性测试：注入不同标准差的高斯噪声，观察误差变化。

---

## 4. 资源与算力

- 论文仅说明所有模型均使用 PyTorch 和 torchdiffeq 实现，采用 AdamW 优化器与 cosine annealing 学习率调度，训练 1000 个 epoch。
- **未明确说明**所使用的 GPU 型号、数量、训练时长、内存消耗等具体算力信息。

---

## 5. 实验数量与充分性

- **实验数量**：
  - 两个混沌系统（Duffing, Van der Pol）的对比实验（表 I）。
  - PMSM 系统的长时域滚动跟踪（图 1）。
  - 输入噪声鲁棒性对比实验（图 2）。
  - 理论上给出了定理证明，无额外的消融实验。

- **充分性评价**：
  - 覆盖了高非线性混沌系统和实际工业驱动系统，体现了方法的通用性。
  - 对比了多个最新基线（5种），属于同领域的标准对比。
  - **不足**：缺乏对收缩参数 \(\beta\)、权重 \(\lambda_c, \lambda_u\) 的消融研究；没有在更多实际系统（如机械臂、自动驾驶）上验证；未进行计算效率或模型复杂度的对比。

- **公平性**：所有方法采用相同软件框架和训练设置，但未提及是否对各基线进行超参数调优；ICNDM额外引入度量网络和多重损失，可能具有更多参数，但论文未进行控制变量对比。

---

## 6. 论文的主要结论与发现

1. **ICNDM在长时域预测上显著优于其他神经微分模型**：混沌系统上200步误差较次优方法（ICODE）降低一个数量级（例如 Van der Pol：1.89e-3 vs 4.38e-2）。
2. **对输入噪声具有极强的鲁棒性**：随着噪声标准差增加，ICNDM误差增长远慢于其他方法，验证了输入到状态收缩理论。
3. **物理系统（PMSM）上的成功验证**：500步滚动仍能紧密跟踪真实轨迹，而标准方法严重漂移。
4. **理论保证具有实践意义**：收缩条件和ISC界并非空谈，在数值实验中确实转化为稳定的预测行为。

---

## 7. 优点

- **创新性强**：将输入依赖的收缩分析融入神经微分方程，解决了非自主系统稳定性保证的空白。
- **理论扎实**：不仅提出算法，还给出了严格的增量指数收敛和ISC定理证明。
- **实验设计全面**：包含混沌系统和真实电机数据，并特别考察了对输入噪声的鲁棒性。
- **工程可落**：模型结构清晰，损失函数由标准项构成，易于在现有框架中实现。

---

## 8. 不足与局限

- **实验覆盖不够广泛**：
  - 缺少消融实验（如移除输入编码器、不同度量初始化、收缩率\(\beta\)的影响）。
  - 未在更多实际受控系统（如机器人、多智能体）上验证。
- **缺乏计算成本对比**：ICNDM相比基线增加了度量网络、输入编码器和三个损失项，但未报告训练/推理时间或参数量。
- **假设限制**：理论部分假设向量场全局Lipschitz连续且度量均匀有界，这些条件在实际中可能难以严格满足。
- **基准方法可能存在不公平**：使用的基线（特别是Standard NODE）可能未针对输入条件进行优化，而ICNDM专门为此设计。
- **数据集规模**：混沌系统数据为仿真生成，PMSM实验未提供数据量细节，无法判断过拟合风险。

---

（完）
